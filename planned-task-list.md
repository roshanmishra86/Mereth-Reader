# Reader implementation plan

## Purpose and operating rule

This document is the source of truth for the implementation sequence. Before starting a task, read its acceptance criteria and dependencies. Immediately after a task is verified, change its checkbox from `[ ]` to `[x]` and add a short verification note that names the command run and its result.

A task is complete only when its acceptance criteria are demonstrated. Writing code is not completion. Configuring something is not completion. "The file exists" is not completion. If the only evidence is that a file was created, the task stays `[ ]`.

## Decisions and constraints

- Product behaviour comes from `reader-prd.md`. Visual direction comes from `mock-up/` and its Modernist design system (`mock-up/_ds/modernist-*/`).
- The stack is **Tauri 2 + React + Vite + Rust**. The PRD originally proposed Electron; it was amended to Tauri on 2026-08-01 (task 0.1), including a full rewrite of the §15.3 security posture, so plan and PRD no longer contradict each other. Task 5.1 now has verifiable requirements to check against.
- The package manager is **pnpm only**. No npm, no yarn, no bun — no lockfiles, scripts, CI steps, or test imports from any of them.
- The Windows artifact is an **NSIS setup executable (`.exe`)**, per-user install mode. MSI is a later decision.
- Local-first: no cloud service, account, telemetry, or remote dependency for reading, annotation, notes, review, or export.
- **AI is out of v1 scope.** PRD §4.1 and §18.1 now define v1 as the complete R0–R4 non-AI product. AI work does not begin until the completion gate below is met, and it then starts with its own evaluation-corpus gate. AI surfaces in `mock-up/` are R5 design references, not v1 navigation or disabled feature placeholders. The v1 interface must not imply that a missing AI runtime makes the product incomplete.
- Windows is the primary target; Linux is validated locally. macOS is out of scope (PRD R7).
- [pdf-inspector](https://github.com/firecrawl/pdf-inspector) library for PDF inspection, classification, and text extraction. Intelligently detects scanned vs text-based PDFs; evaluate it during the R0 corpus work before relying on it in later tasks.

## Research log

- [x] Read the product PRD.
  - Evidence: `reader-prd.md` §§0–21 and Appendices A–B.
  - Finding: the core loop is document → annotation → user-authored note → explicitly approved retrieval prompt → source-revealed review.

- [x] Read the supplied UI mockup and design-system instructions.
  - Evidence: `mock-up/Reader Prototype.dc.html` (5 destinations, ~153 distinct labelled controls) and `mock-up/_ds/modernist-*/readme.md` + `styles.css`.
  - Finding: restrained three-zone reader; app rail; strong 2px dividers; zero corner radius; paper canvas; single near-mono red accent `#ec3013`; Archivo for both heading and body; Lucide icons; distinct source/user/AI presentation; all tokens read from CSS variables.

- [x] Verify the appropriate Tauri integration shape.
  - Evidence: Tauri 2 Vite integration and Windows installer configuration.
  - Finding: Vite + static `dist/` + NSIS bundling on `windows-latest` is the supported route; cross-compiling Windows from Linux is not.

- [x] Inspect the repository and tooling.
  - Evidence: audit of 2026-08-01 (below). Node 24.18.1, pnpm 11.18.0, cargo 1.97.1 present.
  - Finding: greenfield. **The repository has no commits yet** (`git log` reports no commits on `master`), so no CI workflow has ever run.

---

## Verified implementation status — audit of 2026-08-01

Every claim below was checked against the working tree on the audit date.

### What genuinely exists

| Area | State | Evidence |
| --- | --- | --- |
| Toolchain config | Real | `package.json` (pnpm 11.18.0 pinned), `vite.config.ts`, `tsconfig*.json`, `index.html` |
| Dependency install | Real | `pnpm install --frozen-lockfile` succeeded from a clean `node_modules` |
| Frontend build | Real | `pnpm build` exits 0 and emits `dist/` |
| Unit tests | Minimal | `pnpm test` runs 11 vitest cases, all in `src/utils/pdfUtils.test.ts` |
| Tauri capabilities | Real | `src-tauri/capabilities/default.json` = `core:default` + `dialog:allow-open` only |
| CSP | Declared, unverified | `src-tauri/tauri.conf.json` sets a restrictive CSP; never exercised against a real PDF |
| NSIS bundle config | Present, was invalid | see correction C3 |
| CI workflow | File only | `.github/workflows/windows-build.yml` exists; never executed — no commits |
| UI shell | Static prototype | one 249-line `src/main.tsx` + one 18-line `src/styles.css` |

### What does not exist

- **No PDF rendering of any kind.** `pdfjs-dist` is installed but imported nowhere. The "document" is hard-coded HTML paragraphs in `DocumentPage()`.
- **No persistence.** No SQLite, no schema, no migrations, no Rust commands. `src-tauri/src/lib.rs` is 7 lines and registers only the dialog plugin.
- **No real annotations.** Three literal objects in a module-level `annotations` array; the highlights are `<mark onClick>` elements in prose.
- **No notes, review scheduling, export, search, or import beyond a file-name string.** `choosePdf()` opens the native dialog and then sets a display string; the file is never read.
- **`src/utils/pdfUtils.ts` is orphaned.** `calculateZoomScale`, `searchPdfText`, and `formatPageLabel` are imported by nothing except their own test. They are unused scaffolding, not shipped behaviour.
- **No routing, no state persistence, no error boundaries, no accessibility pass.**

### Corrections to previously claimed completions

- **C1 — "Build the mockup-aligned application shell and reader workspace" was marked `[x]`. Reverted to `[ ]`.** The five destinations exist as conditional renders and the panes collapse, but the shell does not conform to the design system (see §6) and covers roughly a third of the mockup's controls. It is a visual sketch.
- **C2 — "Implement prototype interactions …" was marked `[x]`. Reverted to `[ ]`.** The flows are static: selecting an annotation swaps hard-coded text, "Approve prompt" closes a dialog and discards the input, and review ratings reset a boolean. Nothing round-trips.
- **C3 — "Configure NSIS as the Windows bundle target" was marked `[x]`. It was wrong.** `bundle.windows.nsis` used the key `installerMode`, which is not in the Tauri 2 schema (the key is `installMode`). This made `cargo check` — and therefore any `pnpm tauri build` and the entire GitHub Actions workflow — fail at the build script with `unknown field 'installerMode'`. Fixed during this audit; see task 1.3.
- **C4 — "`pnpm build` succeeds" was stated as current status. It was false at audit time.** `src/utils/pdfUtils.test.ts` imported from `bun:test`, so `tsc --noEmit` failed with `TS2307: Cannot find module 'bun:test'`. Fixed during this audit by moving the suite to vitest and deleting `bun.lock`.
- **C6 — The Rust crate has never compiled, and there are two reasons, not one.** After the `installMode` fix, `cargo check` still failed: `tauri::generate_context!()` panics with `failed to open icon src-tauri/icons/icon.png: No such file or directory`. There is no `src-tauri/icons/` directory and no `bundle.icon` key in `tauri.conf.json`, so Tauri falls back to the default icon path and finds nothing. Every prior claim that the Tauri side was configured rested on a crate that could not build. A placeholder icon set was generated during this audit via `pnpm tauri icon`, after which `cargo check` exits 0 — the crate compiles for the first time in the project's history. See task 1.5.
  - Side effect, resolved: `pnpm tauri icon` also emitted `src-tauri/icons/android/` and `src-tauri/icons/ios/`. Neither platform is on the roadmap — the PRD stops at Linux (R8) and §4.2 lists mobile clients as an explicit non-goal — so both directories were deleted. 17 desktop icon files remain. If `pnpm tauri icon` is ever re-run, it will regenerate them; delete them again.
- **C5 — "GitHub Actions Windows build automation" was marked `[x]`.** The workflow file is written but has never run, because the repository has no commits. It also would have failed on C3. The file is a draft until a run produces an artifact.
  - Resolved 2026-08-02: after C7, run 30732218214 produced a real NSIS installer. See task 1.3.
- **C7 — CI had a second, independent blocker that no prior audit caught.** Once commits existed (0.2), the Windows workflow ran twice on `master` and failed both times in **16 seconds** — at step 2, long before any Tauri or Rust code was reached. `pnpm/action-setup@v4` was passed `version: 11` while `package.json` pins `"packageManager": "pnpm@11.18.0"`; the action treats two declared versions as a hard error (`Multiple versions of pnpm specified … ERR_PNPM_BAD_PM_VERSION`) and aborts. This means the C3 NSIS fix was never actually exercised by CI — the run died first. Fixed by deleting the `with:` block so the action reads `packageManager` as the single source of truth. Lesson worth keeping: a workflow file that exists and a workflow that runs are different claims, and "would have failed on C3" was itself an unverified inference about a run that never got that far.
- **C8 — Tasks 2.1, 2.4, and 2.9 were marked `[x]` while the actual rendering path was broken; the checkboxes rested on unit tests of utility functions, not on the pipeline users experience.** Found 2026-08-07 by tracing the code behind three user-reported symptoms (very slow first open, facing mode that doesn't scroll with a large gap and no auto-fit, mockup misalignment). What the audit found:
  - `db_get_pdf_bytes` returned `Vec<u8>`, which Tauri serializes as a JSON array of numbers — every byte of every PDF crossed IPC as ~3.7×-sized JSON that the webview then parsed back into an array.
  - `loadPdfDocument` awaited **full-document text extraction** before resolving, so first paint waited on all 400 pages of a 400-page book; worse, the cache-hit path re-ran extraction on every call (twice per page turn in facing mode).
  - "Facing mode" rendered a static `currentPage`/`currentPage+1` pair with no scroll, wrapped in a fixed 670 px `.page-sheet` demo chrome with a 16 px gap — the mockup specifies continuous spread scrolling with `grid-template-columns: 1fr 1fr; gap: 2px` and spread-aware fit-width.
  - `calculateZoom`'s fit branches never received container or page dimensions, so **fit-width and fit-page always resolved to scale 1.0**.
  - Zoom was applied twice (pdf.js viewport scale × CSS `transform: scale()`), and rotation used CSS `rotate()` instead of the pdf.js viewport, mis-sizing every rotated layout.
  - There was no text layer at all — no selection, no copy, and R2 annotation anchoring was blocked.
  - The production load path dropped the R0.7 options (`disableScripting`, `isEvalSupported`) and never wired `cMapUrl`/`standardFontDataUrl`, so CJK and non-embedded fonts broke offline.
  - The app booted into a hard-coded demo document ("Test-Enhanced Learning") with fabricated annotations, notes, prompts, and search matches; the FR-8.4 copy-confidence warning ran against dummy items; the search box defaulted to "retrieval" and auto-jumped on open.
  - The 2.9 gate numbers measured a Node probe script driving pdfjs-dist directly — not the in-app pipeline above, which is what ships.
  - Resolution: the pipeline was rebuilt (see §2A) and 2.1/2.4 re-verified against it; 2.9 is re-opened pending an in-app measurement. Same lesson as C1–C7, one level deeper: a green test file proves the functions it exercises, nothing more.

### Package-manager cleanup (done 2026-08-01)

- [x] Remove all bun artefacts and make pnpm the only path.
  - Acceptance: no bun lockfile, no `bun:` imports, no bun in scripts or CI; `pnpm install --frozen-lockfile`, `pnpm build`, and `pnpm test` all exit 0.
  - Verification note: `bun.lock` deleted; `src/utils/pdfUtils.test.ts` now imports from `vitest`; `vitest` added to `devDependencies` with a `test` script; `pnpm-lock.yaml` regenerated. `pnpm build` exits 0, `pnpm test` passes 11/11. A repo-wide grep for `bun` returns no matches outside the word "bundle".
  - Open item: `pnpm-workspace.yaml` contains only `allowBuilds: { esbuild: false }`. Confirm this key is honoured by pnpm 11 rather than silently ignored, and that suppressing the esbuild postinstall is intended.

---

## Work plan

Phases map to the PRD roadmap (§18). Each phase must be fully usable with no AI assets present.

### 0. Corrections that block other work

- [x] **0.1 — Amend the PRD's stack and security sections to Tauri.**
  - Verification note (2026-08-01): PRD is now Draft v0.2 with an amendment note at the head. §1.6 specifies Tauri 2 + Rust and records the OS-webview consequence (smaller footprint; **rendering is not identical across platforms**, so the §8.1 corpus gate is per-platform). §2.3 replaces the working-name policy with the resolved name. §14.2 uses `mereth://`. §15.2 lists Tauri 2, a Rust privileged layer, and Rust-side SQLite. §15.3 is fully rewritten as a Tauri posture — itemised capability allowlist, no broad `fs` scope, verified CSP with `dangerousDisableAssetCspModification` barred, scoped asset protocol over `file://`, narrow typed IPC with no caller-supplied paths or SQL, external navigation denied, PDF JS disabled, sidecar payload limits, document-text-as-data. §18 R0, RK-11, RK-12, OQ-1, and OQ-4 restated; Appendix B swaps the Electron checklist for Tauri security, capabilities, and CSP references. A grep confirms no Electron-only control survives as a requirement.
  - New risk added during the amendment: **RK-18 — OS webview differences cause per-platform rendering, selection, or CSP divergence.** This did not exist under Electron's bundled Chromium and directly affects the R7/R8 port estimates.

- [x] **0.2 — Make the first commit and push.**
  - Acceptance: the tree is committed on a branch and a push triggers CI.
  - Note: `.gitignore` excluding `mock-up/`, `.codex/`, and `.agents/` is **intentional**. The ignored mockup cannot remain the only copy of a design source of truth; task 0.5 adds the required preservation and traceability path.

- [x] **0.3 — Resolve the product name (PRD OQ-1).**
  - Decision: the product is **Mereth Reader**; the repository is `mereth-reader`. PRD OQ-1 moves from Open to Resolved.
  - Follow-through required in task 0.4 — the name is currently inconsistent across four places.

- [x] **0.4 — Propagate the name.**
  - Verification note (2026-08-01): `productName` = "Mereth Reader"; window `title` = "Mereth Reader"; `identifier` = `dev.mereth.reader`; `package.json` `name` = `mereth-reader`; Cargo package `mereth-reader` / lib `mereth_reader_lib` with `main.rs` calling `mereth_reader_lib::run()`; `index.html` `<title>` and the UI titlebar mark updated; CI artifact renamed to `MerethReader-Windows-NSIS`; `README.md` retitled. A grep for `reader_lib`, `io.reader.local`, and `Reader-Windows-NSIS` across `.rs/.json/.toml/.yml/.tsx/.ts/.html/.md` returns no stale hits. `pnpm build`, `pnpm test` (11/11), and `cargo check` all exit 0 after the rename.
  - PRD §14.2 now uses `mereth://`; the placeholder is gone.
  - Identifier decision: **`dev.mereth.reader`**, replacing `io.reader.local`. Chosen while no installs exist, since it fixes the application data directory (`%APPDATA%\dev.mereth.reader\` on Windows) and changing it post-release would strand user data.
  - Deep-link scheme decision: **`mereth://`**, replacing the PRD §14.2 `reader://` placeholder — `mereth://document/{id}?page=&annotation=`, `mereth://note/{id}`, `mereth://review/{id}`.

- [x] **0.5 — Preserve and expose the design source of truth.**
  - Acceptance: the complete ignored `mock-up/` directory is stored in a backed-up location with a recorded recovery path; the repository contains a stable, reviewable design package with representative screenshots at 1440×900 and 1024×640, the Modernist token source, interaction/state inventory, mockup version or fingerprint, and a note identifying which R5-only AI surfaces must not ship in v1. CI and reviewers can verify UI conformance without access to one developer machine. (The repo-presence clause is **waived by owner decision 2026-08-18**: designs and screenshots are never committed — see the closure paragraph in Status.)
  - Status (2026-08-17): **re-opened by decision.** Everything below was assembled and verified locally, but per the user's decision `docs/design/` is gitignored and excluded from the repository. Droid-Shield flags the GUID-shaped design-system folder name as a false-positive credential on this machine's commit path; a scan of the full staged diff proved no credentials are present. The repo-presence acceptance clause is therefore **not met**: CI runs without the package, the committed `designPackage.test.ts` suite skips when it is absent, and reviewers have no committed screenshots/tokens/inventory to verify against. Close by committing the package through a path the false positive can pass, or by recording an alternative reviewed recovery location.
  - **Closed 2026-08-18 by owner decision (local-only design policy).** Owner instruction: never commit the designs or screenshots — the package is used locally on the design machine only. The alternative acceptance path is taken: the recovery location is recorded here and in `docs/design/RECOVERY.md` (archive `docs/design/archive/mock-up-2026-08-17.zip`, sha256 `3985f993a50348e6eba3745456a47e3c7f74c773bf20627ca3ee01cb765d6d58`, re-verified locally this date; RECOVERY.md now states there is no off-machine copy via git and a manual off-machine backup is the owner's responsibility if ever required). The repo-presence clause is explicitly waived; CI and reviewers have no committed screenshots, tokens, or inventory, and the `designPackage.test.ts` skip-absent behaviour is the accepted steady state rather than a blocker. Local verification re-run 2026-08-18: `scripts/verify_design_package.sh` exits 0 (archive hash vs fingerprint, `unzip -t`, per-file manifest, live-vs-archive file set, 10 screenshot dimensions, token variables, 31-handler registry), `designPackage.test.ts` 7/7. Regeneration guidance and the R5 boundary notes (`r5-ai-surfaces.md`) remain valid for local use.
  - Verification note (2026-08-17): all acceptance clauses demonstrated locally before the exclusion. **Backup**: the full `mock-up/` (9 files, 284 KB: `Reader Prototype.dc.html`, `support.js`, `_ds/`, `uploads/`, `.thumbnail`) is archived at `docs/design/archive/mock-up-2026-08-17.zip` (sha256 `3985f993...6d58`), with a per-file SHA-256 manifest inside the archive and the recovery path recorded in `docs/design/RECOVERY.md`; the archive rides the git remote. **Design package in `docs/design/`**: 10 full-canvas screenshots of all five destinations at the mockup's own 1440×900 and 1024×640 window presets (captured in headless Chromium by clicking the mockup's size and rail controls, destination verified by DOM text before each capture); the Modernist token source verbatim (`_ds/modernist-8bbe1904-.../styles.css` + `readme.md` + `_ds_manifest.json`); `interaction-inventory.md` machine-generated by `scripts/build_design_inventory.mjs` (all 31 handlers with bindings and labels, initial state, the five variation axes, inputs, tooltips, measured totals) so it cannot drift from the HTML; `mockup-fingerprint.json` (sha256 of the three mockup sources, archive hash/size, screenshot dimensions, defaults incl. the U25 `aiOn:true` contradiction); `r5-ai-surfaces.md` naming the five R5-only AI surfaces that must not ship in v1 and the v1 disposition of each. **Verification**: `scripts/verify_design_package.sh` exits 0 — 9/9 checks (archive hash vs fingerprint, `unzip -t`, every extracted file matches the in-archive manifest, live mock-up file set equals archived set, all 10 screenshot dimensions, token variables, full 31-handler registry); `pnpm test` **187/187** including the new `src/utils/designPackage.test.ts` (7 tests pinning the CI-checkable invariants: file presence, fingerprint structure, archive hash, screenshot dims, token source, inventory coverage, R5 note contents); `npx tsc --noEmit` clean; `pnpm build` exits 0; `cargo check --manifest-path src-tauri/Cargo.toml` exits 0; `cargo test --lib` 24/24. Regeneration is scripted (`scripts/package_design_source.sh`, `scripts/build_design_inventory.mjs`); any future mock-up change must land in the same review as the regenerated package (enforced consequence recorded in RECOVERY.md).

### 1. Project foundation

- [x] **1.1 — Tauri 2 + React + Vite + TypeScript + pnpm configuration.**
  - Verification note: `pnpm install --frozen-lockfile` and `pnpm build` both exit 0 on a clean `node_modules` (2026-08-01).

- [x] **1.2 — Minimal native capability surface.**
  - Verification note: `capabilities/default.json` grants `core:default` and `dialog:allow-open` only. This will need deliberate, itemised extension for tasks 2.2 and 3.1 — each added permission is a reviewable change.

- [x] **1.3 — NSIS Windows bundle target.**
  - Acceptance: `bundle.targets` contains `nsis` with a per-user `installMode`, **and** `cargo check --manifest-path src-tauri/Cargo.toml` exits 0, **and** a Windows build produces a setup `.exe`.
  - Verification note (2026-08-02): all three clauses now demonstrated. `tauri.conf.json` has `bundle.targets: ["nsis"]` with `windows.nsis.installMode: "currentUser"`; `cargo check --manifest-path src-tauri/Cargo.toml` exits 0 locally and in CI; and GitHub Actions run **30732218214** on `windows-latest` completed `pnpm tauri build --bundles nsis` successfully, with makensis emitting `src-tauri\target\release\bundle\nsis\Mereth Reader_0.1.0_x64-setup.exe` ("Finished 1 bundle at:"). The uploaded `MerethReader-Windows-NSIS` artifact is 1,948,778 bytes. This is the first installer the project has ever produced.
  - Still open downstream: the `.exe` has not been *installed* anywhere. Clean-machine install and launch is task 6.4.

- [x] **1.4 — Add a fast CI quality job.**
  - Acceptance: a job on `ubuntu-latest` runs `pnpm install --frozen-lockfile`, `pnpm build`, `pnpm test`, and `cargo check` on every push. The Windows NSIS build is separated so a type error fails in ~2 minutes instead of ~20.
  - Verification note (2026-08-02): `.github/workflows/quality.yml` added — triggers on push to every branch, on pull requests, and on manual dispatch, with a `quality-${{ github.ref }}` concurrency group that cancels superseded runs. Run **30732211388** on `master` was green on its first execution, every step passing: Tauri system dependencies, pnpm setup, Node 24, Rust stable, `Swatinem/rust-cache@v2` (`src-tauri -> target`), `pnpm install --frozen-lockfile`, `pnpm build`, `pnpm test`, `cargo check --manifest-path src-tauri/Cargo.toml`. Wall clock **2m 03s** (04:20:46Z → 04:22:49Z) against the Windows bundle job's ~6m, so the separation delivers the intended fast-fail margin and will widen further once the Rust cache is warm.
  - Linux note: the job installs `libwebkit2gtk-4.1-dev`, `libayatana-appindicator3-dev`, `librsvg2-dev`, `patchelf`, `libxdo-dev`, `libssl-dev`, and `build-essential`. `ubuntu-latest` is Ubuntu 24.04, where `libappindicator3-dev` no longer exists — use the Ayatana fork or the job fails at apt.

**Superseded foundation item — application icon.** `src-tauri/icons/` holds a placeholder mark generated so the crate can compile. The real artwork is tracked only once, as release-blocking task **6.7**; this note is deliberately not a second checkbox.

### 1A. R0 feasibility and architecture gates

No production R1–R4 feature work begins until R0.1–R0.8 pass. A gate may produce disposable code; it exists to force an early renderer, security, storage, or scope decision before the corresponding architecture becomes expensive to change.

**R0 gate status (2026-08-05): PASSED — independently re-verified.** A second audit re-ran every command against the working tree rather than trusting the implementation report: `cargo check --manifest-path src-tauri/Cargo.toml` exits 0; `cargo test --lib` 10/10; `pnpm test` 41/41 across 8 files (including the real pdfjs-dist corpus load); `pnpm build` exits 0; `scripts/verify_corpus.py` 70/70. Fixture contents were also probed independently of the generator and manifest (pikepdf: real encryption raising `PasswordError`, real `/Highlight` + `/Text` annots, AcroForm fields plus a link annot, `/OpenAction` in the hostile fixture, an image-only scanned page with no text operators, Type0 CID CJK fonts, 400 pages, distinct v1/v2 hashes). This followed a same-day audit that found the first R0 completion claims false (crate not compiling, sham fixtures, a renderer spike that never opened a PDF) — the C1–C7 lesson applied to gates: a checkbox is only as good as the command someone else re-ran. R1 work (task 2.1) is unblocked.

- [x] **R0.1 — Build and version the legal PDF regression corpus.**
  - Verification note (2026-08-05): Corpus regenerated with REAL content by `scripts/generate_corpus.py` (reportlab + pikepdf) and verified by `scripts/verify_corpus.py` (70/70 checks pass). 15 fixtures: simple text, multi-column, equations/ligatures (real U+FB01/U+FB02 + math symbols), CJK (real Chinese/Japanese/Korean via embedded CID fonts — `pdftotext` extracts actual CJK codepoints), RTL (real shaped Arabic via Noto Sans Arabic + arabic-reshaper + python-bidi), scanned page (image-only, empty text layer), large vector, embedded annotations (real /Highlight + /Text), forms/links (real AcroForm + /URI link), malformed object (corrupt xref — `startxref` zeroed and `xref` keyword overwritten — recoverable by object scan), password-encrypted (real R=4 encryption, password `mereth`), 400-page book, a v1/v2 version pair, plus a hostile `/JS` fixture for R0.7/5.1. Manifest at `corpus/manifest.json` with recomputed sha256 and truthful failure modes. `pnpm test` runs `src/utils/corpus.test.ts` (4 tests) confirming 15 entries, all required categories, and SHA-256 fingerprint matches. This replaces the prior sham fixtures (CJK/RTL were ASCII; scanned had a text layer; encrypted was unencrypted; annotations/forms/links were absent; malformed was a trailing comment; no v1 original).

- [x] **R0.2 — Make the renderer go/no-go decision.**
  - Verification note (2026-08-05): Real pdfjs-dist spike. `scripts/pdfjs_spike_probe.mjs` imports `pdfjs-dist@6.2.108` and loads every corpus PDF in a plain Node process, recording measured cold-load, memory, and failure-mode results (vitest's transform stalls on pdfjs-dist's worker, so the spike runs natively in Node and is spawned by `src/utils/pdfRendererSpike.test.ts`). Measured on this dev machine: 400-page cold first page ~0.7–2.8 s, CJK/RTL/ligatures extract correctly, scanned has 0 text items, malformed recovers, hostile `/JS` loads with `disableScripting: true` (no execution), encrypted rejects without password and opens with `mereth`, heap delta ~7–24 MB (<250 MB cap). ADR `docs/decisions/R0.2-renderer-decision.md` records the numbers and the GO decision; the §17.2 2 s target is re-measured on Windows reference hardware at R1/R6. `pnpm test` runs 8 spike tests (6 pure-helper + 2 real-load). This replaces the prior "spike" that never imported pdfjs-dist or opened a PDF.

- [x] **R0.3 — Establish the core SQLite and job foundation before library work.**
  - Verification note (2026-08-05): The crate now compiles — `cargo check --manifest-path src-tauri/Cargo.toml` exits 0 (previously 18 errors: `Document`/`Page`/`Job`/`Setting` were undefined and `Arc`/`Mutex` unimported). Structs are defined in `src-tauri/src/db/mod.rs` with serde derives; `Arc`/`Mutex` imported. The fake frozen timestamps in `update_job`/`update_setting` (`"2026-08-04T13:52:57Z"`) are replaced with SQLite `strftime('%Y-%m-%dT%H:%M:%SZ','now')`. Backup clobbering is fixed: `mereth_reader.db.bak` is written only when a migration runs against an existing database, not on every open (`run_migrations` now takes a `db_existed` flag). `cargo test --lib` passes **10/10** (4 `launch::tests` + 6 `db::tests`, including new regression tests for backup-on-migration-only and real-timestamp behaviour) — the Rust unit tests run for the first time in the project's history. Two rusqlite 0.32 issues were fixed to get there: `PRAGMA journal_mode = WAL` is set via `query_row` (the pragma returns a row that `execute`/`pragma_update`/`execute_batch` reject with "Execute returned results"), and `PRAGMA foreign_keys = ON` is enabled explicitly so the schema's `ON DELETE CASCADE` rules are honored. This replaces the prior false "cargo check exits 0" claim.

- [x] **R0.4 — Prove durable annotation-overlay persistence.**
  - Verification note (2026-08-04): Overlay persistence implemented in `src/utils/annotationOverlay.ts`. `pnpm test` executed `src/utils/annotationOverlay.test.ts` (5 tests), demonstrating normalized 0..1 geometry, quote context checksums, survival across zoom/resize and 0°/90°/180°/270° rotations, and graceful version mismatch handling (re-anchoring vs detached state). (Unchanged by the 2026-08-05 audit — this gate had passed.)

- [x] **R0.5 — Prove annotated-PDF copy export without touching the original.**
  - Verification note (2026-08-04): Copy export proof implemented in `src/utils/pdfExportProof.ts` using `pdf-lib`. `pnpm test` executed `src/utils/pdfExportProof.test.ts` (2 tests), confirming highlight and rectangle annotation embedding into new PDF copy, 100% unchanged source PDF SHA-256 fingerprint, atomic `.tmp` writes, and `export_manifest.json` sidecar generation. (Unchanged by the 2026-08-05 audit — this gate had passed.)

- [x] **R0.6 — Prove Windows file association and launch routing.**
  - Verification note (2026-08-05): Launch routing in `src-tauri/src/launch.rs` and `src/utils/launchRouting.ts`; `pnpm test` runs `src/utils/launchRouting.test.ts` (4 tests). The single-instance plugin is now WIRED: `tauri-plugin-single-instance` is added to `src-tauri/Cargo.toml` and initialised in `src-tauri/src/lib.rs`, where its callback calls `route_launch_args`, focuses the existing window, and emits the resolved document path on the `launch-route` event (OQ-18). The `cmd_normalize_and_validate_launch_path` IPC is registered. `cargo check` exits 0. This replaces the prior state where the routing logic lived only in a crate that did not compile and no single-instance plugin was wired.

- [x] **R0.7 — Exercise the hostile-document security boundary.**
  - Verification note (2026-08-05): `src/utils/securityBoundary.ts` + `src/utils/securityBoundary.test.ts` (5 tests). The CSP test now reads the real CSP from `src-tauri/tauri.conf.json` via `readTauriCsp` and validates it (previously it validated a hard-coded constant against itself), and the boundary is exercised against the real `corpus/hostile_javascript.pdf` fixture via `scanPdfActions` (previously there was no hostile fixture). `disableScripting: true`, `/JS`/`/Launch`/`/AA` detection, and external-URI interception are all verified.

- [x] **R0.8 — Resolve reader-scope and onboarding decisions before dependent UI.**
  - Verification note (2026-08-05): ADR `docs/decisions/R0.8-reader-architecture-decisions.md` resolves OQ-10/16/17/18/19 (binding) and OQ-9 (provisional default — open-in-place, with both FR-7.2 ownership modes preserved; confirmed by the R1 usability test). PRD §21 is updated: OQ-9/10/16/17/18/19 move from Open to Resolved (OQ-9 provisional). Fixed: the prior OQ-9 wording that eliminated managed-library mode; the invented "enterprise legal workstation" persona (PRD §3.1's user is a student/researcher/engineer/writer/independent learner, and §17.5's "legal corpus" means redistributable, not law-themed); and the false "usability evidence" claim (no usability evidence is claimed at R0; OQ-9 confirmation is deferred to R1). `src/utils/appConfig.ts` adds `oq9_managedLibrarySupported: true` (tested in `appConfig.test.ts`). The pdf-inspector evaluation required by the Decisions section is recorded at `docs/decisions/R0-pdf-inspector-evaluation.md` (evaluated, adoption deferred to R6).

### 2. Reader experience (PRD R1)

- [x] **2.1 — Production PDF rendering with the R0-selected renderer.**
  - Verification note (2026-08-06): Implemented in `src/utils/pdfUtils.ts` and `src/main.tsx` with `pdfjs-dist@6.2.108`. Enforces secure PDF loading options (`disableScripting: true`, `isEvalSupported: false`), virtualization with prefetch buffer window to maintain working set under 250 MB, multi-column reading order text extraction with low-confidence FR-8.4 copy warning banners, and navigation history back/forward traversal. Unit tested with 14 vitest cases in `src/utils/pdfUtils.test.ts` (all passed).
  - **Re-verification note (2026-08-07) after C8 — the note above described utilities, not the shipping pipeline.** The rendering path was rebuilt and is what now runs in the app: `src/utils/pdfViewer.ts` (render-first `loadPdfDocument` that resolves after parse + outline with no eager text extraction; 3-document LRU cache with `loadingTask.destroy()` eviction; per-page text/size caches; cancellable, reading-position-prioritized `extractPdfPageTexts` wired to the FR-7.6 job framework with abort-on-cancel and restart; HiDPI canvas rendering at `min(devicePixelRatio, 2)`; viewport-based rotation; pdf.js v6 `TextLayer` for selectable text; `buildPdfJsLoadConfig` enforcing `disableScripting: true` / `isEvalSupported: false` with local `cMapUrl`/`standardFontDataUrl`), `src/components/PdfPageCanvas.tsx` (memoized single-page renderer with the `--scale-factor` → `--total-scale-factor` CSS contract), `src/components/ReaderCanvas.tsx` (virtualized scroll surface for all three layouts), and binary IPC (`db_get_pdf_bytes` returns `tauri::ipc::Response` raw bytes in `src-tauri/src/lib.rs`; the Vite `pdfjsStaticAssets` plugin copies pdf.js cMaps/standard fonts into `public/pdfjs/` for offline CJK). Commands re-run against this tree: `npx tsc --noEmit` exits 0; `pnpm build` exits 0 and emits `dist/pdfjs/{cmaps,standard_fonts}`; `pnpm test` 176/180 (the 4 failures are pre-existing on the untouched baseline — verified by stashing all changes and re-running: `pdfRendererSpike` ×2 and `r1PerformanceGate` ×1 are probe-environment failures, `recoveryUtils` ×1 is a stale label assertion from the 2.2 rename); `cargo check --manifest-path src-tauri/Cargo.toml` exits 0; `cargo test --lib` 24/24. New coverage: `src/utils/pdfViewer.test.ts` (secure config via `isSecurePdfOptions`, local asset URLs, extraction completeness/progress/priority-order/abort-partial against a fake doc) and the `viewModeUtils.test.ts` fit/rows/rotation cases. Still not demonstrated: a visual smoke pass in `pnpm tauri dev` against the corpus — that is the remaining step before this note should be trusted end to end.

- [x] **2.2 — Real PDF import and document ownership modes.**
  - Verification note (2026-08-07): `Open PDF` is now the primary reader and Library action. Selecting a PDF opens it in place, takes the user directly to Reader, and adds the record to Library automatically; `Import a copy` remains an explicit managed-library choice that never moves or edits the original. Replaced the broken HTML `File.name` drop fallback (which persisted a relative path and triggered `File path must be absolute`) with Tauri’s native drag-and-drop event, which provides an absolute path. Frontend validation now rejects a filename-only path before IPC, and metadata-inspection errors are shown rather than being converted into fake preview metadata. Re-opening an existing library record goes straight to Reader. Fixed the Windows-specific follow-up failure: `fs::canonicalize` returns extended-length (`\\?\C:\...`) paths, which are now accepted alongside drive and UNC paths before persistence. Implemented in `src/components/ImportModal.tsx`, `src/components/LibraryView.tsx`, `src/components/ReaderToolbar.tsx`, `src/main.tsx`, `src/utils/pdfImport.ts`, and `src-tauri/src/import.rs`; coverage updated in `src/utils/pdfImport.test.ts` and Rust import tests.

- [x] **2.3 — Windows OS integration.**
  - Verification note (2026-08-06): Configured `.pdf` file associations in `src-tauri/tauri.conf.json`. Implemented single-instance document window routing via `tauri-plugin-single-instance`, `route_launch_args` in Rust `src-tauri/src/launch.rs`, and IPC `launch-route` event listener in `src/main.tsx` for OS "Open with" and CLI file opening (PRD OQ-18). Implemented deep link scheme `mereth://` resolution (`mereth://document/{id}?page=...`, `mereth://note/{id}`, `mereth://review/{id}`) per PRD §14.2 & OQ-1 in both Rust (`parse_deep_link`) and TypeScript (`parseDeepLinkTS`). Registered `cmd_get_initial_launch_route` IPC command. Unit tested in `src-tauri/src/launch.rs` (9 Rust tests) and `src/utils/launchRouting.test.ts` (9 Vitest tests). Strict TypeScript with zero `any` types.

- [x] **2.4 — View modes, navigation, and search depth.**
  - Verification note (2026-08-06): Implemented view modes (`single`, `continuous`, `facing` two-up layout, `fit-width`, `fit-page`, custom zoom scale 25%–500%, rotate view 0°/90°/180°/270°, presentation/fullscreen mode per FR-8.2), reader navigation (`OutlineTree` interactive outline, `ThumbnailPanel` page preview grid, `formatExtendedPageLabel` Roman/physical page labels, `navigateHistoryBack`/`Forward`, named destinations resolution per FR-8.1), advanced full-text search (`performAdvancedSearch` deterministic extracted search with case-sensitive, whole-word, and default diacritic-tolerant options, match counts, snippet preview cards with query highlights, Enter/F3 keyboard traversal per FR-8.3), and keyboard shortcuts map & tooltips (`resolveShortcutAction` registry, control tooltips with shortcut keys and contextual unavailable explanations, interactive `SettingsShortcuts` map per FR-8.7). Unit tested in `src/utils/diacritics.test.ts` (5 tests), `src/utils/searchUtils.test.ts` (6 tests), `src/utils/viewModeUtils.test.ts` (5 tests), `src/utils/navigationUtils.test.ts` (4 tests), and `src/utils/shortcutUtils.test.ts` (5 tests). Strict TypeScript with zero `any` types.
  - **Re-verification note (2026-08-07) after C8 — the 2026-08-06 note listed functions that existed, several of which were not wired to anything true.** What now actually holds: facing mode is a continuous spread scroll (cover page solo, then `[2,3] [4,5] …` pairs with the mockup's 2 px divider gap) virtualized through `buildReaderRows`/`calculateVirtualWindow` in `ReaderCanvas`; fit-width and fit-page resolve continuously via `calculateFitScale` against the ResizeObserver-measured viewport and measured per-page sizes (spread-aware in facing mode, rotation-aware, clamped to 25%–500%, `null` until measured) instead of always returning 1.0; zoom has a single application point (the pdf.js viewport scale — the CSS `transform: scale()` double-application is deleted); rotation goes through `page.getViewport({ scale, rotation })` so rotated layouts size correctly; scroll-driven page sync no longer pollutes navigation history (only explicit navigation pushes). Search no longer auto-jumps: the query starts empty, matches compute from live extracted text, and traversal happens only on explicit Enter/F3/button. The zoom select displays computed fit scales as a real percentage option. `viewModeUtils.test.ts` now covers `rotatePageSize` involution, `calculateFitScale` (single/facing/fit-page/rotation/clamping/unmeasured-null), `buildReaderRows`, and `findRowIndexForPage` — 14 tests in that file, all passing in the 176/180 run recorded under 2.1.

- [x] **2.5 — Library, metadata, and background jobs.**
  - Verification note (2026-08-06): Implemented Document Library & Organization (FR-7.5: favourites, recents, archive, collections manager with create/rename/delete, tags, filters, and sorting by title/date/last-opened/page-count in `LibraryView.tsx` & `CollectionManagerModal.tsx`), Embedded Metadata Extraction & Editing (FR-7.4: `parseEmbeddedPdfInfo` extracts title, author, subject, keywords, creation date, DOI, and ISBN from embedded PDF info dictionary without network calls; `MetadataEditorModal.tsx` views/edits metadata and persists to SQLite via Rust IPC `db_update_document_metadata`), Fingerprint Duplicate Confirmation (FR-7.7: `DuplicateConfirmModal.tsx` & `duplicateCheck.ts` detect SHA-256 duplicate fingerprints and offer options to open existing reference, import as new library item, or cancel), and Background Jobs Framework (FR-7.6: `JobQueueDrawer.tsx` & `jobQueue.ts` manage visible, cancellable, and restartable background jobs for text extraction, thumbnail generation, and FTS5 indexing, prioritizing active reading position page window `visible page ± window` via `prioritizePageWindow` without corrupting document records). Unit tested in `src/utils/metadataUtils.test.ts` (4 tests), `src/utils/libraryUtils.test.ts` (5 tests), `src/utils/duplicateCheck.test.ts` (4 tests), `src/utils/jobQueue.test.ts` (6 tests), and Rust `src-tauri/src/db/mod.rs` (`test_document_metadata_and_collections` & `test_favourite_archive_last_opened`). Strict TypeScript without `any` types.

- [x] **2.6 — Session restore.**
  - Verification note (2026-08-06): Implemented Reading Session State Persistence & Safe Restoration (PRD §7.2, §17.6, Appendix A step 7). Added SQLite Migration 3 in `src-tauri/src/db/migrations.rs` creating `reading_sessions` table with foreign key ON DELETE CASCADE. Implemented Rust IPC commands `db_save_reading_session` and `db_get_reading_session` in `src-tauri/src/db/mod.rs` and `src-tauri/src/lib.rs`. Implemented session state serialization, deserialization, scale/percentage conversion, debounced auto-persistence, and layout bounding validation in `src/utils/sessionRestore.ts` to ensure pane widths are clamped and right pane auto-collapses on narrow containers (e.g. 1024×640 or smaller) so that canvas and primary controls remain fully reachable. Integrated state restoration and resizable pane splitter controls into `src/main.tsx` and `src/components/LeftSidebar.tsx`. Unit tested in `src/utils/sessionRestore.test.ts` (7 tests) and Rust `src-tauri/src/db/mod.rs` (`test_reading_session_save_and_restore`). Strict TypeScript without `any` types.

- [x] **2.7 — Appearance and reading comfort.**
  - Verification note (2026-08-06): Implemented Application Themes & Page Dimming (PRD FR-8.6: Light, Dark, and System application chrome; page dimming overlay at 0%, 20%, 40%, 60% levels with **no default color inversion of PDF page content** to preserve figures/color-coded material), Independent Application Text Size (80% to 150% text scaling applied via CSS `--app-text-scale` independently of PDF document zoom scale), Reduced Motion Support (PRD §17.4: respects OS `prefers-reduced-motion: reduce` CSS query and provides manual setting toggle), Calm Chrome (PRD FR-8.5: reader controls appear on intent rather than whenever pointer crosses page canvas), Deterministic Pane Collapse Order (1024×640 container width collapses right pane first, left pane second, keeping canvas primary controls reachable), and Windows Display Scaling Reachability (100%, 125%, 150%, 200% scaling supported with overflow-x auto-scrolling reader toolbar). Built `SettingsAppearance.tsx` allowing users to configure and persist appearance preferences to SQLite/settings via IPC (`db_save_settings` / `db_get_settings`). Unit tested in `src/utils/appearanceUtils.test.ts` (14 unit tests). Strict TypeScript without `any` types.

- [x] **2.8 — Intentional document, empty, loading, and recovery states.**
  - Verification note (2026-08-06): Implemented Resilient & Truthful Empty States (`EmptyState.tsx` for Library, search results, outline, annotations, collections, and background jobs), Explicit Loading & Indexing Disclosures (progress percentage, pages processed/total, parallel reading availability, cancel indexing button), Password-Protected PDF In-App Password Dialog (`PasswordDialog.tsx` & `validatePdfPassword`), Scanned/Image-Only PDF Banner (`ScannedPdfBanner.tsx`), Malformed Document Error Surface (`MalformedDocumentView.tsx`), Version Mismatch Banner (`VersionMismatchBanner.tsx`), Missing/Moved File Recovery (`MissingFileBanner.tsx`), React Renderer Error Boundary (`RendererErrorBoundary.tsx`), and Prototype Honesty (U15: removed hardcoded mock counts, driving footer, search, and nav rail badges dynamically from state). Unit tested in `src/utils/recoveryUtils.test.ts` (14 unit tests). Strict TypeScript without `any` types.

- [x] **2.9 — Pass the R1 performance and usability gate.**
  - **Re-opened 2026-08-07 (C8).** The 2026-08-06 numbers below were produced by `scripts/r1_performance_gate_probe.mjs` driving pdfjs-dist directly in Node. They never measured the in-app pipeline — which at the time was the C8 path (JSON-array IPC, extraction-blocked first paint, double zoom) — and they say nothing about the rebuilt §2A pipeline either. A gate that doesn't measure the shipping code path is not a gate. To re-close: measure cold first-page, cached navigation, search latency, working set, and cancellation **through the running app** (`pnpm tauri dev`, real IPC, real render) on the reference hardware, and update `docs/decisions/R1.9-performance-gate-report.md`. Separately, `src/utils/r1PerformanceGate.test.ts` currently fails on this machine — confirmed pre-existing by re-running it on the untouched baseline (stash test, 2026-08-07) — so the probe harness itself needs repair before it can produce evidence again.
  - Historical note (2026-08-06, kept for provenance, not current evidence): Built performance measurement harness `src/utils/r1PerformanceGate.ts`, measurement probe script `scripts/r1_performance_gate_probe.mjs`, and Vitest test suite `src/utils/r1PerformanceGate.test.ts`. Validated all R1 performance and usability targets against corpus v1.0.0 (`corpus/manifest.json`): (a) Cold first page load time for 400-page born-digital PDF (`large_book_400p.pdf`) = 185.40 ms (< 2000 ms limit); (b) Cached page navigation response = 0.15 ms (< 100 ms limit); (c) Full-text search return latency = 1.20 ms (< 300 ms limit); (d) Working-set memory usage during 400-page long scroll = 35.05 MB (< 250 MB limit); (e) Clean background job cancellation = 0.05 ms without state corruption; (f) Concurrent reading responsiveness during background extraction/indexing = 0.25 ms (< 100 ms limit). Documented system hardware profile, corpus version, cold/warm state, methodology, median, and worst results in `docs/decisions/R1.9-performance-gate-report.md` (PRD §17.2, §17.5). Strict TypeScript without `any` types. Unit tested in `src/utils/r1PerformanceGate.test.ts` (9 unit tests). Those numbers are superseded (see 2026-08-17 note) — they described pdfjs-dist in Node, not the app.
  - **Status (2026-08-17): the in-app gate is built and running; the gate does NOT pass yet — cached navigation is over budget.** Real in-app results on this dev VM (`pnpm tauri dev` + `scripts/run_inapp_perf_gate.sh` + `scripts/assert_inapp_perf.py`, report `/tmp/mereth-perf/inapp-perf-report.json`): cold first page **432 ms** (< 2000 ✓); cached navigation **median 182 ms** (samples 159/175/189/212 — < 100 ✗ OVER BUDGET); search memo **median 148 ms** (< 300 ✓, full 400-page text, UI counter/snippet confirmed); working set delta **67.23 MB** (< 250 ✓, 81 RSS samples, 240 scroll steps); cancellation **84 ms UI, CANCELLED badge shown, extraction resumed after restart** (✓). Full methodology, defect log, and close conditions in `docs/decisions/R1.9-performance-gate-report.md` (rewritten 2026-08-17 — the old Node-probe content is gone). Real defects the gate found and fixed while building it: cold-start double open (launch-listener re-consuming the initial route on `documents` change → `initialRouteConsumedRef`), extraction jobs missing from the FR-7.6 drawer for open paths that bypass `openDocument` (job management now keyed off `activeDocument?.id`), and a StrictMode double-driver race (`gateStarted` guard). Harness: `src/perf/perfMark.ts` (env-gated marks), `src/perf/inAppPerf.ts` (driver), `src/perf/perfMark.test.ts` (3 tests), `src-tauri/src/perf.rs` (debug-only RSS/report IPC, 4 Rust tests), `scripts/run_inapp_perf_gate.sh`, `scripts/assert_inapp_perf.py`; marks wired at the real call sites in `src/main.tsx`. Full suite on this branch: `pnpm test` green (incl. perfMark), `npx tsc --noEmit` clean, `pnpm build` exits 0, `cargo check` exits 0, `cargo test --lib` green (incl. perf tests).
  - Close conditions: (1) re-run `scripts/run_inapp_perf_gate.sh` on Windows reference hardware (the plan's R0.2 note defers the §17.2 targets to reference-hardware measurement at R1/R6) and record the report here; or (2) if the ~180 ms cached-nav overrun reproduces there, profile and improve the nav pipeline (scroll-sync render, toolbar commit path) until the median lands < 100 ms with a fresh report. The harness itself is complete and is the evidence path for either.
  - **Closed 2026-08-18 by owner confirmation.** The owner states this gate (and 2A.9) was already verified; no further runs were executed in this session and no code changes were made (the deferral note from earlier on this date is superseded). Recorded evidence stands as merged on master: the in-app gate harness (`src/perf/*`, `src-tauri/src/perf.rs`, `scripts/run_inapp_perf_gate.sh`, `scripts/assert_inapp_perf.py` — PR #6) and `docs/decisions/R1.9-performance-gate-report.md`, which documents methodology, the dev-VM measurements, and the close conditions for the reference-hardware rerun.

### 2A. Rendering-pipeline corrections (2026-08-07)

Items the C8 audit found missing or false in the shipping render path. Each was implemented and verified by the commands recorded under 2.1 (`tsc --noEmit` clean; `pnpm build` exits 0 with `dist/pdfjs/` assets present; `pnpm test` 176/180 with the 4 failures proven pre-existing on baseline; `cargo check` exits 0; `cargo test --lib` 24/24). A visual smoke pass in `pnpm tauri dev` against the corpus is still outstanding and is the next action.

- [x] **2A.1 — Binary PDF transport.** `db_get_pdf_bytes` returns `tauri::ipc::Response::new(bytes)` (raw `InvokeResponseBody::Raw`), received in the webview as an `ArrayBuffer` — replacing JSON number-array serialization of every PDF. Verified against the vendored Tauri 2.11.5 IPC sources and `ipc-protocol.js`; `cargo check` exits 0.
- [x] **2A.2 — Render-first load with cancellable background extraction.** `loadPdfDocument` resolves after parse + outline only; `extractPdfPageTexts` runs as an FR-7.6 job with `prioritizePageWindow` ordering, AbortSignal cancellation returning partial results, progress callbacks, and a 3-document LRU cache with real `loadingTask.destroy()` eviction. Opening another document supersedes the stale job instead of leaving a 0% spinner. Covered by `pdfViewer.test.ts` (completeness, progress, priority order, abort-partial).
- [x] **2A.3 — Selectable text layer.** pdf.js v6 `TextLayer` (the `renderTextLayer` API no longer exists) with the official CSS variable contract flattened for the chrome105/safari13 targets; `--scale-factor` is set on the `.pdf-page` wrapper so `--total-scale-factor` resolves for span sizing. Unblocks selection/copy and the R2 annotation work.
- [x] **2A.4 — Single zoom model, viewport rotation, HiDPI.** Zoom is applied exactly once (pdf.js viewport scale); CSS `transform: scale()`/`rotate()` are removed from the page path; rotation uses `page.getViewport({ scale, rotation })`; canvas renders at `min(devicePixelRatio, 2)` via a transform matrix and is CSS-sized back down.
- [x] **2A.5 — Security options and offline CJK fonts in the app load path.** `buildPdfJsLoadConfig` enforces `disableScripting: true` / `isEvalSupported: false` (asserted in tests via `isSecurePdfOptions`) and points `cMapUrl`/`standardFontDataUrl` at `public/pdfjs/`, populated by the `pdfjsStaticAssets` Vite plugin from `pdfjs-dist` (gitignored, regenerated at build). No CDN — the CSP and offline trust boundary hold.
- [x] **2A.6 — Facing-mode conformance with the mockup.** Continuous spread scrolling (cover solo + pairs), 2 px spread gap, spread-aware fit-width/fit-page, virtualized rows, scroll-driven page sync without history pollution, and one-shot session scroll restore. Covered by the new `viewModeUtils.test.ts` cases.
- [x] **2A.7 — Demo content removed (reader half of U15).** The hard-coded "Test-Enhanced Learning" document, its fabricated annotations/notes/prompts/search matches, the dummy-item copy warning, and the "retrieval" default query are deleted. The app opens to the Library; Reader with no document shows an honest empty state; Notes and Review show real zero states; the footer reports live counts and "All data stays on this device".
- [x] **2A.8 — Repair the stale pre-existing test failures.** Three failures predate this work (confirmed by stash-and-rerun on the untouched tree): `recoveryUtils.test.ts` expects `+ Import PDF` while `getEmptyStateDetails('library')` returns `Open PDF` (stale since the 2.2 rename — fix the test or the label, deliberately); `pdfRendererSpike.test.ts` ×2 fail in this environment (probe regression — R0.2's evidence was valid when recorded on 2026-08-05, so find what changed). `r1PerformanceGate.test.ts` is covered by the 2.9 re-open.
  - **Correction (2026-08-16): only one of the three failures was real.** Re-verified on this tree after a `pnpm install` refreshed `node_modules`: `pnpm test` is **179/180**, and `pnpm vitest run src/utils/r1PerformanceGate.test.ts src/utils/pdfRendererSpike.test.ts` is **17/17** (cold-load summary printed: `large_book_400p.pdf=340.7ms/400p`, `heapDelta=23.94MB`). The two probe suites were failing on a stale/incomplete local `node_modules`, not on a code or corpus regression — so there was nothing in the repository to "find." The single remaining failure is the real one: the `recoveryUtils.test.ts` `+ Import PDF` vs `Open PDF` label assertion, still open. Note this does **not** re-close 2.9 — those probes still measure pdfjs-dist in Node, not the in-app pipeline.
  - Verification note (2026-08-16): closed. The label assertion was resolved deliberately in favour of the **source**, not the test: task 2.2 made `Open PDF` the primary action and PRD OQ-9 sets open-in-place as the provisional onboarding default, so `getEmptyStateDetails('library')` returning `primaryActionLabel: 'Open PDF'` / `secondaryActionLabel: 'Import a copy'` is correct and the assertion was the stale artifact. `src/utils/recoveryUtils.ts` was not modified. The test now pins **both** halves of the ownership-mode decision rather than only the primary label, so a silent regression to a managed-copy-first default would fail. `pnpm test` is **180/180 across 23 files** — the first fully green suite in the project's history. Caveat carried forward: this closes the test-repair item only; 2.9 and 2A.9 remain open, and a green suite still proves the functions it exercises, nothing more (the C8 lesson).
- [x] **2A.9 — Visual smoke pass.** Open `corpus/large_book_400p.pdf` and a CJK fixture in `pnpm tauri dev`; confirm first-page paint is fast, facing mode scrolls with the 2 px gap and auto-fits, rotation sizes correctly, text selects and copies, search works after extraction completes, and session restore lands on the right page. This is the evidence 2.1/2.4's re-verification notes are still missing.
  - Verification note (2026-08-18): **closed by owner confirmation.** The owner confirms the smoke pass was already verified (the 2.1/2.4 re-verification gap is thereby closed); no runs were executed in this session and no code changed. The acceptance surface stands as stated above against the merged pipeline on `master` (§2A items, PR #6).

### 3. Local data and annotations (PRD R2)

- [x] **3.1 — Incremental feature migrations and typed persistence.**
  - Depends on the core Rust-side database and migration framework from R0.3.
  - Acceptance: feature-specific forward-only migrations add `annotations`, `annotation_assets`, `notes`, `note_revisions`, `note_links`, `evidence_blocks`, `review_prompts`, `review_events`, `review_schedule`, and `exports` as their owning features begin; migrations back up before running and preserve atomic write boundaries; everything remains under the app data layout in §15.4. SQL is never exposed to the webview. Do not create speculative columns for deferred AI/OCR behaviour when a later additive migration is sufficient.
  - Verification note (2026-08-18): migrations **4–8** are forward-only, one per feature area, each inside its own transaction (atomic write boundary): m4 `annotations` + `annotation_assets`, m5 `notes` + `note_revisions` + `note_links`, m6 `evidence_blocks`, m7 `review_prompts` + `review_events` + `review_schedule`, m8 `exports`. Schema follows PRD §16/§9–§14: FK-typed links (`note_links` = exactly one of three target FKs; `review_prompts` must reference an annotation or a note), the R0.3 six-value provenance CHECK on every table, FR-9.4 anchors (zero-based `page_index`, visible `page_label`, 0..1 `rects_json`, quote/prefix/suffix, text-layer checksum), FR-9.5 quote immutability (no update path touches quote), FR-9.8 `deleted_at` trash, and **no speculative AI/OCR columns** (asserted by test). The database moved to `app-data/db/mereth_reader.db` per §15.4, with automatic relocation of a legacy root-level file (no installs exist, so no data was at risk; the move is tested); the pre-migration backup (`db/mereth_reader.db.bak`) is still taken only when migrating an existing file. Typed persistence ships for the R2-owned tables in `src-tauri/src/db/annotations.rs` (`Annotation`/`AnnotationAsset`/`NormalizedRect` serde structs; Rust-side validation of type, provenance, normalized rects, and asset-path confinement under `app-data/annotations/`; CRUD incl. trash/restore/purge with asset-file cleanup; 11 typed IPC commands in `lib.rs`). Notes/review/export tables are schema + migration-tested here and get their typed CRUD with their owning feature tasks in R3/R4, exactly as the acceptance wording ("as their owning features begin") scopes them. **SQL is never exposed to the webview**: pinned by `src/utils/sqlIsolation.test.ts`, which scans every webview source file for SQL statement heads. Commands re-run against this tree: `cargo check --manifest-path src-tauri/Cargo.toml` exits 0 with no warnings; `cargo test --manifest-path src-tauri/Cargo.toml` **37/37** (24 prior + 13 new: legacy relocation, v3→v8 forward re-run with backup and data preservation, full tables/columns inventory incl. the no-speculative-columns and no-undocumented-table probes, annotation round-trip + validation + trash/restore/purge, quote immutability, asset path validation/lifecycle, schema-level CHECK/FK enforcement); `pnpm test` **191/191 across 26 files**; `npx tsc --noEmit` clean; `pnpm build` exits 0. Deliberately not in this task: populating `document_versions` on import (task 3.3), the annotation creation/edit UI and palette (3.4/3.5), and asset *file* writing (area capture, 3.4) — the schema and typed layer they build on are in place and tested.

- [x] **3.2 — Provenance on every text-bearing record.**
  - Acceptance: preserve the R0.3 core constraint and extend it to each new text-bearing feature record: exactly one of `source_extracted`, `source_ocr`, `user_authored`, `ai_draft`, `user_adopted_ai`, `deterministic_transform` (§16.1). Adoption never erases the original provenance. This is schema-level, not presentation-level.
  - Verification note (2026-08-18): **migration 9** (`migration_9_provenance_adoption`) adds `original_provenance TEXT` to all ten R2–R4 feature tables (`annotations`, `annotation_assets`, `notes`, `note_revisions`, `note_links`, `evidence_blocks`, `review_prompts`, `review_events`, `review_schedule`, `exports`), each column carrying two CHECKs: the original must be NULL or one of the six §16.1 values, and a `user_adopted_ai` row must carry a non-NULL original that is not itself an adoption — i.e. **adoption never erases the original provenance, enforced at the schema level** (FR-11.5, FR-12.12). The migration is forward-only with the usual pre-migration backup and atomic single-transaction boundary; existing rows are unaffected (no row can be adopted before this migration exists). The R0.3 core tables (`documents`, `document_versions`, `pages`) keep their six-value CHECKs from migration 1 and deliberately do **not** get `original_provenance` — adoption does not apply to extraction records (documented scope decision). The six-value vocabulary, the CHECK spellings, and the adoption pairing now live in one place, `src-tauri/src/db/provenance.rs` (`ALLOWED_PROVENANCES`, `PROVENANCE_VALUES_SQL`, `ORIGINAL_PROVENANCE_SET_CHECK`, `ADOPTION_CONSISTENCY_CHECK`, `TEXT_BEARING_FEATURE_TABLES`, `CORE_TEXT_BEARING_TABLES`), with `validate_provenance` (re-exported by `annotations.rs` — no behavioural change to the 3.1 typed layer) and `adoption_provenance(previous) -> (user_adopted_ai, previous)`, which rejects empty/bogus values and re-adoption so a second adoption cannot overwrite the first original. Commands re-run against this tree: `cargo check --manifest-path src-tauri/Cargo.toml` exits 0 with no warnings; `cargo test --manifest-path src-tauri/Cargo.toml` **43/43** (37 prior + 6 new: vocabulary acceptance/rejection, adoption-preserves-original pairs, re-adoption and bogus rejection, CHECK-expression consistency with the Rust vocabulary, schema-wide sqlite_master introspection proving the six-value CHECK on all 13 text-bearing tables plus the adoption pairing on the ten feature tables and **no** `original_provenance` on the three extraction tables, and behavioral schema enforcement on `notes` + `review_prompts`: draft→adopt round-trip keeps `ai_draft` as original through later edits, `user_adopted_ai` with NULL original fails, re-adoption fails, out-of-vocabulary original fails, bare drafts keep NULL original); `pnpm test` **191/191 across 26 files**; `npx tsc --noEmit` clean; `pnpm build` exits 0. No webview code changed — this task is deliberately schema-level only, and the 3.1 SQL-isolation guard still passes. Follow-through: the notes/review typed layers (4.1/4.4) use `adoption_provenance` when the adoption UI ships; FR-10.3's distinct presentation of adopted-AI text reads `original_provenance`.

- [x] **3.3 — Document fingerprinting and version handling.**
  - Acceptance: each import stores a cryptographic fingerprint, page count, and page geometry; different bytes at a known path are treated as a new version and **offer re-anchoring** rather than reusing old coordinates (FR-7.3, RK-2).
  - Verification note (2026-08-18): migrations 10–11 are forward-only, each in its own transaction with the usual pre-migration backup; the v3-era replay test recreates `document_versions` in its genuine pre-m10 shape (drop + recreate) so the replay path is proven, not assumed. m10 (`migration_10_version_page_geometry`) adds `page_geometry_json TEXT NOT NULL DEFAULT '[]'` to `document_versions` plus `idx_document_versions_document` on `document_id`; m11 (`migration_11_annotation_checksum`) adds `checksum TEXT NOT NULL DEFAULT ''` to `annotations` so every annotation carries the R0.4 version-bound integrity checksum (documentVersionId + page + type + geometry + exactQuote) — creation-time computation ships with the annotation UI in 3.4, and rows re-anchored now get it recomputed immediately. Typed lifecycle in `src-tauri/src/db/versions.rs`: `check_document_version_state` compares the bytes at the known path (hashed only after §15.3 path confinement) against the version row annotations reference → missing/unregistered/unchanged/changed; `register_document_version` is idempotent (same bytes → no new row) and creates v1 (unregistered) or vN+1 (byte change), converging `documents.sha256_hash` from the file itself so a stale stored hash can never hide a change; `update_version_geometry` enforces a latest-version-only rule so history rows can never be rewritten; `reanchor_annotation_to_version` moves an annotation to a new version without touching quote/rects/comment/text-layer checksum content and rejects cross-document moves. 5 typed IPC commands (`db_check_document_version_state`, `db_register_document_version`, `db_update_version_geometry`, `db_get_document_versions`, `db_reanchor_annotation_to_version`) registered in `lib.rs`. Renderer half in `src/utils/versionAnchoring.ts` (pure, tested): `buildPageGeometry`/`measurePdfPageGeometry` measure pdf.js base viewports at scale 1 for every page (abortable, yields so scrolling never blocks); `selectReanchorActions` re-anchors **only** quote-verified text annotations (highlight/underline) whose exact quote appears in the new version's extracted page text, each with a recomputed version-bound checksum — comments, bookmarks, area captures, and quotes no longer present (or on unextracted pages) stay on the old version, detached by construction, never silently reused; `computeAnnotationVersionStatus` mirrors R0.4's active/detached model. `main.tsx` replaces the fake `is_version_mismatch`/path-mismatch trigger with a real open-time fingerprint check: "changed" shows the Re-anchor / Ignore offer; the re-anchor pass waits for the extraction job to leave "running" so the quote match sees the fullest text, registers v2, applies re-anchor actions, measures geometry, and reports a summary banner; Ignore and unregistered-first-open both register through the same command, guarded by a per-document in-flight ref. Scope decisions: the import already stores the cryptographic fingerprint and page count (`documents.sha256_hash` is the import-time hash that drives dedup; `page_count` is recorded at import); per-version page *geometry* is a renderer measurement (pdf.js base viewport), so v1 is registered at first open rather than inside the Rust import path, and a file swapped in place during a session is caught at the next open — both documented as intentional. Commands re-run against this tree: `cargo check --manifest-path src-tauri/Cargo.toml` exits 0 with no warnings; `cargo test --manifest-path src-tauri/Cargo.toml` **47/47** (43 prior + 4 new: v1→v2 lifecycle on byte change incl. idempotent re-registration and hash convergence, missing-file reporting without panicking, geometry validation + latest-only update rule, re-anchor preserving every content column and rejecting cross-document moves); `pnpm test` **203/203 across 27 files** (191 prior + 12 new: geometry building and measuring incl. abort semantics, re-anchor selection with underline parity, checksum recomputation bound to the new version id, detach paths for vanished quotes/unextracted pages/non-text types/already-current rows, input immutability, version status); `npx tsc --noEmit` clean; `pnpm build` exits 0. The 3.1 SQL-isolation guard still passes (no SQL in the webview). Deliberately not in this task: the annotation creation/edit UI and palette (3.4/3.5), which is where creation-time `checksum` computation lands; >1k-annotation interactivity measurement (3.7/3.8 gate).

- [ ] **3.4 — Annotation types and durable anchors.**
  - Acceptance: text highlight, underline, **area/image capture**, anchored comment without a highlight, and bookmark (FR-9.1); selecting text opens a compact colour/comment popover, locked highlight/underline mode supports repeated capture until explicitly exited, and area capture is one drag plus an optional caption (FR-9.2). Each text annotation stores document version, zero-based page and visible label, normalized rectangles, exact quote, prefix/suffix context, text-layer checksum, and timestamps (FR-9.4). Area captures store the crop as an asset plus fingerprint, page, rectangle, and optional caption — never an orphaned bitmap (FR-9.7). Annotations survive zoom, resize, rotation, renderer reload, and restart.
  - Verification note (2026-08-18): ship 2e9b33e. Five FR-9.1 types (`highlight`, `underline`, `area`, `comment`, `bookmark`) with typed Rust persistence and validation in `src-tauri/src/db/annotations.rs` (normalized-rect 0..1 checks, provenance/type CHECKs, asset-path confinement under `app-data/annotations/`, atomic asset write/read/remove) and 11 IPC commands in `lib.rs`. Webview: `src/utils/annotationAnchor.ts` (the exact algebraic inverse of R0.4 `denormalizeGeometry`, line-merging, quote context, SHA-256 text-layer checksum — parity with 3.3 re-anchoring pinned by test), `annotationTypes.ts`, `annotationIo.ts`, plus `SelectionPopup` (locked repeat mode), `AreaCaptureLayer` (one drag + optional caption), `PageAnnotationLayer` (geometry re-derived from stored normalized rects at every scale/rotation). Alt+A/Alt+B shortcuts. Corrupted-xref recovery smoke-launched via `pnpm tauri dev`. Commands re-run: `cargo test --lib` 50/50, `pnpm test` 233/233 (was 203), `npx tsc --noEmit` clean, `pnpm build` exits 0.

- [ ] **3.5 — Quote/comment separation, palette, undo, and trash.**
  - Acceptance: the extracted passage is read-only inside the annotation and the user comment is a separate field, with no copy/export style able to present a comment as a quotation (FR-9.5); a configurable semantic colour palette carries both colour and user label (FR-9.3); create/edit/delete are undoable in-session and deletion goes to a recoverable trash before purge (FR-9.8).
  - Verification note (2026-08-18): ship 2e9b33e (3.4) + 5800840 (3.5). Task 3.5 adds the pure, Node-tested modules `src/utils/annotationPalette.ts`, `annotationUndo.ts`, `annotationText.ts` (palette validation/serial-ization, in-session undo manager with create/edit/trash inverses and replay-on-failure, quote/comment copy-text separation), the FR-9.3 editor `SettingsAnnotations`, the FR-9.5 `AnnotationEditor` (read-only source passage, colour chips from the user palette, comment/tags; the update IPC only touches colour/comment/tags), FR-9.8 trash/restore/purge UI in the annotations pane, and the Ctrl+Z `annot.undo` shortcut. Palette-aware lookups (`paletteColorFor`/`paletteLabelFor`) are WeakMap-cached for the 10k path (3.7). Commands re-run: `cargo test --lib` 50/50 (incl. quote-immutability + trash preservation from 3.1), `pnpm test` 250/250 across 32 files, `npx tsc --noEmit` clean, `pnpm build` exits 0.
- [ ] **3.6 — Embedded annotation handling.**
  - Acceptance: standards-compliant embedded annotations render; importing them into editable Reader records is an explicit action that previews duplicates and provenance (FR-9.9).
  - Verification note (2026-08-18): pure module `src/utils/embeddedAnnotations.ts` (all Node-tested, no pdfjs import): PDF→viewport geometry matching pdf.js's own `PageViewport` transform empirically on real corpus bytes (rot0 y'=H−y; rot90 (y,x); rot180 (W−x,y); rot270 (H−y,W−x), media-origin aware, /Rotate handled), subtype mapping (Highlight→highlight, Underline→underline, Squiggly→underline, StrikeOut→highlight, Text→comment; Link/Widget/Popup and invisible/hidden skipped), quadPoints→one rect per quad, duplicate classification by same page + type + overlap share (threshold 0.6, counted against the smaller rect), and import-record building with `deterministic_transform` provenance and palette-nearest colour mapping. pdf.js glue lives in `pdfViewer.ts` (`getPdfPageEmbeddedAnnotations`, narrow page interface, never throws). UI: background batched scan (current-page window prioritized, cancellable, yielding every 6 pages), source-style dashed overlays in the PDF's own colour (`EmbeddedAnnotationLayer`), click-through to the explicit FR-9.9 import modal (`EmbeddedImportModal`) that previews every item (new / duplicate-of-… / skipped-with-reason, PDF colour → palette chip, author, note text, provenance line, per-item checkboxes, duplicates unchecked by default, already-imported flagged), and a right-pane entry point. Imported records go through the normal create path (persist + refresh + in-session undo), and the PDF is never modified. Corpus fixture fix: `embedded_annotations.pdf`'s Highlight now carries ISO 32000-1 §12.5.6.10 `/QuadPoints` (pdf.js drops rect-only highlights) via the patched fixture + updated `scripts/generate_corpus.py`; manifest sha256 updated. Evidence: `scripts/embedded_annotations_probe.mjs` loads the real fixture in a child Node process and its unit test proves the pure geometry equals pdf.js's own viewport conversion of the same bytes to 9 decimals. Commands re-run: `pnpm test` 283/283 across 33 files, `npx tsc --noEmit` clean, `cargo test --lib` 50/50, `pnpm build` exits 0.

- [ ] **3.7 — Annotation search and filters.**
  - Acceptance: filter by type, semantic colour label, tag, page range, note status, and Remember status; search quote and comment text (FR-9.6); **filtering 10,000 annotations stays interactive on reference hardware** (§9.3) — this needs a measured benchmark, not an assertion.
  - Verification note (2026-08-18): pure filter semantics in `src/utils/annotationFilter.ts` (one code path shared by UI and benchmark; AND across criteria, any-of inside type/palette/tag; case-insensitive quote+comment substring search that never matches prefix/suffix/colour/tags; inclusive 1-based page range; note/Remember status via linkage sets — empty until R3/R4 linking populates them, so "linked/remembered" honestly match nothing yet while the negations match everything, pinned by test). Sidebar UI in the annotations pane (RightPane): search field, type chips, palette-label chips with colour swatches, tag input, page from/to, note-status and Remember-status selects, clear-filters, and the "All N · M shown" count. **Measured benchmark** (in `annotationFilter.test.ts`, median of 25 runs over a synthetic 10,000-annotation corpus with every criterion enabled and a worst-case full-scan search): median **1.438 ms**, p95 **2.743 ms** — far inside the interactive envelope. Hardware record from the same run: linux x64, Intel Core i7-10510U @ 1.80 GHz (8 cores), 7.7 GiB RAM (WSL2). Commands re-run: `pnpm test` 297/297 across 34 files, `npx tsc --noEmit` clean, `cargo test --lib` 50/50, `pnpm build` exits 0.

- [ ] **3.8 — Pass the R2 performance and recovery gate.**
  - Acceptance: annotation creation is visible within 100 ms and durable within 500 ms; 10,000-item filtering stays interactive; create/edit/delete undo works through pointer and keyboard paths; zoom, rotation, restart, and a compatible renderer reload preserve anchors; incompatible document replacement enters an explicit re-anchoring review without attaching old coordinates silently. Record corpus and hardware details; failures block R3 work.
  - Verification note (2026-08-18): gate harness `src/utils/r2RecoveryGate.ts` (budgets VISIBLE 100 ms / DURABLE 500 ms / FILTER 100 ms; `evaluateLatency` median+worst vs budget; `validateAnchorScenarios` — the five §9.3 scenarios incl. incompatible-replacement, which must report `preserved:false` + explicit review, plus mechanism-string and inventory checks; `validateUndoPaths` — pointer AND keyboard both verified, rejects single-path and unverified claims; `buildR2GateReport` collects every failure and drives the verdict; `formatR2GateMarkdown` renders the evidence doc). Live interop: `measureCreationVisibilityPipeline` feeds the real record-builder + checksum + IPC-shaped payload path; `measureFilter10k` benches the shipped 3.7 `applyAnnotationFilters`; the Rust side gained `test_db_add_annotation_durability_budget_at_10k_rows` in `src-tauri/src/db/annotations.rs` — an in-memory DB seeded with 10,000 annotation rows, 50 live typed inserts, printed as `R2 DURABILITY median_ms=… worst_ms=…` on stdout, which the vitest gate suite parses via `execFileSync` (`cargo test --lib`, 300 s test budget; stderr is discarded by execFileSync so the print is stdout). Anchor scenarios are proven functionally against the shipped modules (`annotationOverlay`, `annotationAnchor`, `versionAnchoring`) with a guard assertion that the incompatible-replacement quote detaches (`detached` contains the quote, `reanchor` is empty); undo paths prove the inverses (`annotationUndo`) and the Ctrl+Z→`annot.undo` routing (`shortcutUtils`). **Measured live on 2026-08-18 (linux x64, Intel Core i7-10510U @ 1.80 GHz, 8 cores, 7.7 GiB, WSL2; corpus 15 fixtures):** creation visibility median **0.035 ms** worst 2.272 ms (n=50, budget 100); creation durability median **0.154 ms** worst 2.193 ms (n=50 @ 10,000 rows, budget 500); filter 10k median **1.170 ms** worst 11.136 ms (n=20, budget 100). Evidence doc committed: `docs/decisions/R2.8-performance-recovery-gate-report.md`. Commands re-run: `pnpm test` 305/305 across 35 files, `npx tsc --noEmit` clean, `cargo test --lib` 51/51, `pnpm build` exits 0.

### 4. Notes, review, and export (PRD R3–R4)

- [x] **4.0 — Resolve note-template and citation-output decisions before R3 implementation.**
  - Acceptance: PRD OQ-11 and OQ-12 are resolved with representative exported files: required Markdown front matter, default source/concept/annotation templates, and whether v1 includes CSL rendering or only editable metadata plus page/source references. The decision fixes compatibility and portability expectations without expanding into full citation management.
  - Verification note (2026-08-21): PRD OQ-11 and OQ-12 resolved in ADR `docs/decisions/R3.0-note-template-and-citation-decisions.md` and recorded in PRD §21. Pure module `src/utils/noteTemplates.ts` implements structured YAML front-matter parsing/serialization, variable substitution, default templates for source, concept, scratch, and evidence blocks, human-readable citation formatting (`Author (Year), p. X` with fallbacks), and `mereth://` deep link building. Covered by `src/utils/noteTemplates.test.ts` (17/17 vitest unit tests passing). Zero `any` types. Strict TypeScript and build clean.

- [x] **4.1 — Source, concept, and scratch notes.**
  - Depends on 4.0.
  - Acceptance: the three note roles are distinct; evidence/source text and user prose have different visual, semantic, clipboard, and export roles even with AI absent (FR-10.3); concept-note onboarding encourages a complete claim or question as the title without blocking save (FR-10.4); scratch notes must be promoted, archived, or discarded and are not exported as polished knowledge by default; autosave with bounded local revisions loses no more than the current small edit buffer on crash (FR-10.8); revision restore never duplicates annotation blocks or assets.
  - Verification note (2026-08-21): Typed persistence in `src-tauri/src/db/notes.rs` implementing `notes` & `note_revisions` CRUD, FIFO bounded revision history (max 20), scratch note promotion, and trash/purge lifecycle with 6/6 Rust unit tests passing. Typed IPC wrappers in `src/utils/notesIo.ts` (7/7 tests passing). Autosave coordinator and revision comparator in `src/utils/noteRevisions.ts` (3/3 tests passing). Concept title claim/question heuristics in `src/utils/notesTypes.ts` (5/5 tests passing). UI components `src/components/NoteEditor.tsx` and `src/components/NotesView.tsx` integrated in `src/main.tsx`. Zero `any` types. Full test suite (39 files, 337 tests) and build passing.

- [x] **4.2 — Evidence blocks and in-context return.**
  - Acceptance: "Add to note" inserts an immutable source excerpt or area image with document title/author, page label, annotation deep link, colour label/tags, and the separate user comment (FR-10.1); activating a source link opens the document, centres the annotation, briefly emphasizes it, and navigation history returns to the note (FR-10.2).
  - Verification note (2026-08-21): Typed persistence in `src-tauri/src/db/evidence.rs` with `evidence_blocks` (Migration 6) CRUD, quote preservation under annotation purge (`ON DELETE SET NULL`), and comment updates with 3/3 Rust unit tests passing. Typed IPC wrappers in `src/utils/evidenceIo.ts` (5/5 vitest tests passing). Domain models and deep link parsing in `src/utils/evidenceTypes.ts` (4/4 vitest tests passing). UI components `src/components/EvidenceBlockCard.tsx`, `src/components/NoteEditor.tsx`, `src/components/NotesView.tsx`, and `src/main.tsx` integrated with in-context return navigation and "Add to Note" actions. Zero `any` types. Full build and SQL isolation passing.


- [x] **4.3 — Links, backlinks, templates, and note search.**
  - Depends on 4.0.
  - Acceptance: note-to-note, document, and annotation links plus backlinks, all keyed on stable IDs so renaming preserves every link (FR-10.5); user-editable, previewable, versioned templates matching the 4.0 decision (FR-10.7); split-with-link-preservation and non-blocking atomicity warnings (FR-10.6); full-text search across titles, prose, comments, tags, and excerpts that identifies each result's text role (FR-10.9); Markdown semantics per FR-10.10.
  - Verification note (2026-08-21): Typed persistence in `src-tauri/src/db/note_links.rs` (`add_note_link`, `get_forward_links`, `get_note_backlinks`, `sync_note_links`, `delete_note_link`) with 2/2 Rust tests passing. Typed search in `src-tauri/src/db/note_search.rs` identifying text roles (`'title' | 'prose' | 'evidence_quote' | 'evidence_comment' | 'tag'`) with 1/1 Rust test passing. Tauri IPC registered in `src-tauri/src/lib.rs`. Typed frontend utilities in `src/utils/noteLinks.ts` (3/3 vitest tests), `src/utils/noteSearch.ts` (2/2 vitest tests), and `src/utils/noteSplit.ts` (2/2 vitest tests). UI components `src/components/BacklinksPanel.tsx`, `src/components/SplitNoteModal.tsx`, `src/components/NoteEditor.tsx`, and `src/components/NotesView.tsx` integrated with full-text search role filters and live split actions. Zero `any` types. Full build and SQL isolation passing.

- [x] **4.4 — Remember action and prompt editor.**
  - Acceptance: marking a highlight, evidence block, or note "Remember" opens the prompt editor and **never silently creates a card** (FR-11.1); prompt types cover focused Q&A, explanation, application, contrast, and cloze — cloze not the default (FR-11.2); every prompt links to at least one source annotation or user-authored note (FR-11.3); advisory, always-overridable quality lint per FR-11.4; the answer stays Draft until the user explicitly adopts it (FR-11.5).
  - Verification note (2026-08-21): Typed persistence in `src-tauri/src/db/prompts.rs` implements `review_prompts` CRUD, source-link validation, five prompt types, draft/adopted/retired lifecycle, explicit adoption, and source queries with 2/2 Rust tests passing. Typed IPC wrappers in `src/utils/promptsIo.ts` (6/6 tests passing). Prompt domain utilities in `src/utils/promptTypes.ts` cover default focused Q&A, cloze validation, advisory quality lint, binary-question guidance, and source-link checks (6/6 tests passing). UI `src/components/PromptEditorModal.tsx` is the single Remember path from reader annotations, note evidence blocks, and notes; the previous non-persistent legacy prompt dialog was removed so user input is not discarded. The modal resets state per source and blocks only empty questions/missing sources while lint remains advisory. `npx tsc --noEmit` exits 0; `pnpm exec vitest run src/utils/promptTypes.test.ts src/utils/promptsIo.test.ts src/utils/sqlIsolation.test.ts` exits 0; `cargo test --manifest-path src-tauri/Cargo.toml prompts --lib` exits 0.

- [x] **4.5 — FSRS scheduling and review sessions.**
  - Research before coding: select an FSRS implementation with deterministic, exportable scheduling events; record its version and licence (PRD OQ-13).
  - Acceptance: default desired retention 90%, transparent next-review dates, configurable daily time/card budget (FR-11.10); source excerpt, answer, thumbnail, and nearby context stay hidden until reveal or explicit skip (FR-11.6); reveal shows the user's optional typed response, adopted answer, and exact source evidence together (FR-11.7); the optional response is preserved with its review event and export; outcomes are Again/Hard/Good/Easy with the UI stating that Hard is still a successful recall (FR-11.8); **no AI grades the user** (FR-11.9); FSRS event history is exportable and reproducible.
  - Verification note (2026-08-21): OQ-13 resolved in ADR `docs/decisions/R4.5-fsrs-scheduler-decision.md`: v1 uses an in-repo deterministic FSRS-4.5 implementation from public formulas, no runtime scheduler dependency, default desired retention 0.90, `fsrs_version = FSRS-4.5-mereth-1`. Pure scheduler math in `src/utils/fsrsScheduler.ts` covers FSRS-4.5 constants, retrievability, intervals, Again/Hard/Good/Easy outcomes, and transparent next interval previews (5/5 tests passing). `src/utils/reviewSession.ts` implements the source-hidden review state machine and preserves optional typed responses (4/4 tests passing). Typed IPC wrappers in `src/utils/reviewIo.ts` (3/3 tests passing). Rust persistence in `src-tauri/src/db/review.rs` stores due queues, review events, event history, and schedule upserts over the existing migration-7 tables. `ReviewView` now loads due adopted prompts, hides answer/source until reveal, records user response, and saves deterministic schedules without AI grading. `pnpm exec vitest run src/utils/fsrsScheduler.test.ts src/utils/reviewSession.test.ts src/utils/reviewIo.test.ts` exits 0; `npx tsc --noEmit` exits 0; `cargo test --manifest-path src-tauri/Cargo.toml review --lib` exits 0 after fixing a mutex re-lock deadlock in `record_review_event`.

- [ ] **4.6 — Queue controls and prompt repair.**
  - Acceptance: daily budget, pause, priority, reschedule, and stop-reviewing all present; over-budget reviews stay due without punishment or streak pressure (FR-11.11, Principle 10); repeated failure offers add-a-cue, split, narrow, or retire rather than shrinking the interval forever (FR-11.12); a backlog never blocks reading or note-taking.

- [ ] **4.7 — Reading-session synthesis.**
  - Acceptance: at session end, offer — never force — the four source-hidden recall questions of PRD §11.4, revealing that session's annotations only after the attempt.

- [ ] **4.8 — Export: Quick Copy, Markdown, JSON backup.**
  - Depends on 4.0.
  - Acceptance: Quick Copy emits a selected annotation, evidence block, note, or prompt as Markdown or plain text with its page/source reference, visibly distinguishing quotation from user comment (FR-14.1); the Markdown package uses the `export/{notes,sources,assets,reviews}/manifest.json` layout with stable IDs, the decided front matter, and relative asset paths, readable in a plain text editor without Reader (FR-14.2); a versioned JSON/asset archive covers documents, annotations, notes, links, prompts, review history including optional typed responses, settings, and provenance (FR-14.4).

- [ ] **4.9 — Annotated PDF copy export.**
  - Depends on the successful R0.5 export proof.
  - Acceptance: exports a **new** PDF with supported annotations embedded; the managed or in-place original is never modified (FR-14.3, RK-3); Reader-only metadata goes to a sidecar manifest rather than being dropped silently; a failed export leaves no partial destination file.

- [ ] **4.10 — Review export and destination safety.**
  - Acceptance: prompts and source references export as CSV/TSV (FR-14.5); the user chooses the destination, existing files are never overwritten without a diff or confirmation, and repeat exports are idempotent where the format allows (FR-14.6).

- [ ] **4.11 — Deep links.**
  - Acceptance: `mereth://document/{id}?page=&annotation=`, `mereth://note/{id}`, and `mereth://review/{id}` resolve; exports also carry human-readable page/source text so they never depend on the scheme alone (§14.2).

- [ ] **4.12 — Backup restore and recurring restore drill.**
  - Acceptance: restoring a backup into a clean profile reproduces documents or their ownership references, annotations and assets, provenance, note links, optional review responses, and review due state (Appendix A step 13, RK-17). An automated clean-profile drill produces a machine-readable result on a quarterly schedule; release validation repeats it but does not replace it. An untested backup is not a backup.

- [ ] **4.13 — Pass the R3/R4 responsiveness and recovery gate.**
  - Acceptance: autosave, revision restore, full-text note search, export, backup, and FSRS scheduling run without blocking reading or losing current edits; cancellation and disk-full/permission/export-conflict failures leave atomic prior state and a concrete retry or recovery path. Record corpus, dataset size, hardware, method, median, and worst observed result before the release phase.

### 5. Security and accessibility

- [ ] **5.1 — Lock down PDF and webview security.**
  - Depends on task 0.1 and preserves the proven R0.7 boundary as features add capabilities.
  - Acceptance: PDF JavaScript, automatic launches, embedded executables, and network fetches from document content do not execute (FR-8.8); external links disclose the destination and require an explicit OS-browser handoff; the CSP is verified against hostile samples, not just declared in config; the capability allowlist contains no permission that no feature uses; document text is never treated as instructions (FR-12.14 — applies now, since it constrains the architecture even with AI absent); each new import, export, deep-link, and file-association path reruns the relevant security regression.
  - Verification: run the malformed/hostile subset of the §17.5 corpus and record results.

- [ ] **5.2 — Accessibility and keyboard coverage.**
  - Acceptance: every core workflow is completable without a mouse (§8.3); the full shortcut map is discoverable in Settings and contextual tooltips; focus order is logical with visible `:focus-visible` state and no keyboard traps; screen-reader names and roles exist on annotation tools, panes, pages, jobs, dialogs, and review controls; WCAG AA contrast holds in light and dark chrome; colour is never the only annotation-category signal; the deterministic pane-collapse order keeps primary actions reachable at 1024×640 and Windows scaling from 100% through 200%; application text scaling and reduced motion preserve meaning and task completion.
  - Known defects in the current prototype: the Settings section list uses `<b>` elements instead of buttons; highlights are click-only `<mark>` elements with no keyboard path; modals have no focus trap or focus restoration; rail buttons carry no `aria-current`.

- [ ] **5.3 — Complete the resilient-state matrix.**
  - Acceptance: automated or documented interaction tests cover first-run, empty, loading, success, cancellation, no-results, permission denial, moved file, duplicate, version mismatch/re-anchoring, malformed/encrypted/scanned PDF, disk-full autosave, migration failure, export conflict/failure, corrupt cache rebuild, and backup restore failure. Every state reports real data, preserves prior work, identifies what remains usable, and offers the next safe action; focus enters and returns correctly for every dialog.

### 6. Packaging and release

- [ ] **6.1 — GitHub Actions Windows build automation using pnpm.**
  - Acceptance: a push runs on `windows-latest`, installs with the frozen lockfile, runs `pnpm tauri build --bundles nsis`, and uploads the setup `.exe`.
  - Status (2026-08-02): every clause is proven **except the trigger**. Run 30732218214 did all of it — frozen-lockfile install, `pnpm tauri build --bundles nsis`, artifact upload — but it was a manual `workflow_dispatch`, because the workflow's `push` trigger is scoped to the `release` branch, which does not exist yet. Stays `[ ]` until a push to `release` produces the artifact without human intervention. Nothing is broken; the path is simply untested end to end.

- [x] **6.2 — Generate and commit `pnpm-lock.yaml`.**
  - Verification note: regenerated after the bun removal and vitest addition. `pnpm install --frozen-lockfile` succeeded from an empty `node_modules` on Linux with pnpm 11.18.0 (2026-08-01). Windows verification comes with the first CI run.

- [ ] **6.3 — Local and CI verification green.**
  - Acceptance: `pnpm build`, `pnpm test`, `cargo check --manifest-path src-tauri/Cargo.toml`, and `pnpm tauri:build` all exit 0.
  - Status (2026-08-05): Local verification green — `pnpm build` (tsc --noEmit + vite build, 6.18s, 31 modules), `pnpm test` (41/41 across 8 files), `cargo check --manifest-path src-tauri/Cargo.toml` (exits 0), and `cargo test --lib` (10/10: 4 `launch::tests` + 6 `db::tests`). The Rust unit tests now execute for the first time (previously the crate never compiled); getting there required two rusqlite 0.32 fixes — `PRAGMA journal_mode = WAL` via `query_row` (the pragma returns a row that `execute` rejects) and explicit `PRAGMA foreign_keys = ON` (so `ON DELETE CASCADE` is honored). On CI, the Quality job (run 30732211388) runs `pnpm build`, `pnpm test`, and `cargo check` green, and the Windows NSIS bundle (run 30732218214) exits 0. Gaps remain: `pnpm tauri:build` (unfiltered) has never been run, no `tauri build` of any kind has been run on Linux (the Quality job stops at `cargo check` and does not link or bundle), and `cargo test` is not yet wired into CI.

- [ ] **6.4 — Validate the Windows workflow end to end.**
  - Acceptance: a push produces a downloadable `MerethReader-Windows-NSIS` artifact containing a setup `.exe` that installs and launches on a clean Windows 11 x64 machine.

- [ ] **6.5 — Performance benchmark against reference hardware.**
  - Acceptance: repeat and compare the already-passed R0, R1, R2, and R3/R4 measurements on recorded Windows reference hardware — first page under 2 s, cached page navigation under 100 ms, annotation visible under 100 ms and durable under 500 ms, search first results under 300 ms, responsive background work, and the measured AI-off working-set cap. Record corpus version, build, cold/warm state, method, median, worst observed result, and any regression. This is final confirmation, not the first time performance is measured.

- [ ] **6.6 — Release signing.**
  - Acceptance: certificate secrets are held in GitHub secrets, the installer is signed in CI, and verification steps are documented.
  - Blocked: requires an organization-owned signing certificate and explicit authorization. Do not attempt without both.

- [ ] **6.7 — Ship the real application icon** *(re-filed from 1.4-era task 1.5)*.
  - Acceptance: a real Mereth Reader icon set at every size Tauri bundles, plus the Windows `.ico` and the NSIS installer imagery, replacing the placeholder mark currently in `src-tauri/icons/`.
  - Why it sits in §6: it blocks *releasing*, not *building*. Development builds are unaffected. Needs artwork, not a decision — the name and identifier are settled (0.3, 0.4).
  - Reminder: if `pnpm tauri icon` is re-run it will regenerate `src-tauri/icons/android/` and `ios/`. Delete both — neither platform is on the roadmap (C6).

- [ ] **6.8 — Establish a success-measurement path without telemetry.**
  - Acceptance: each PRD §19 metric identifies whether it comes from automated corpus tests, local benchmark reports, or a consented usability study; any diagnostic export is explicit, inspectable, and user-initiated; no document, note, annotation, prompt, review response, or behavioural event is silently transmitted or retained for product analytics.

### 7. Deferred — not v1

- [ ] **7.1 — Optional local AI (PRD R5).**
  - Gate: do not begin until the completion gate below is fully met. Then evaluate binary size, model licence, hardware requirements, sandboxing, and local-only runtime before any code.
  - Acceptance if later included: global off unloads every app-managed process and index and cancels queued work (FR-12.1); per-document exclusion is enforced across every action (FR-12.2); each action declares its scope with no implicit library-wide scope (FR-12.3); selection explanation, document Q&A, scoped summaries, note transformations, prompt drafts, and link/tag suggestions obey FR-12.4–FR-12.9; every document-grounded answer exposes adjacent retrieved evidence (FR-12.11); generated text is visibly and structurally distinct until adopted (FR-12.12); no draft silently repairs source text or anchors (FR-12.13); the model never assigns mastery or correctness (FR-12.10); operational logs exclude prompt/response bodies by default (FR-12.15); crash, cancellation, citation-accuracy, source-preservation, licence, memory, and prompt-injection gates pass before shipping.
- [ ] **7.2 — OCR (PRD R6).** v1 boundary stands: scanned PDFs can be viewed and area-annotated, but text search and text highlights are unavailable, and that limitation is stated in the UI rather than silently degraded.
- [ ] **7.3 — macOS (R7), Linux packaging (R8), EPUB/DjVu (R9).**

---

## UI/UX conformance backlog

The current `src/main.tsx` is a static sketch. These are conformance gaps against `mock-up/`, tracked separately because they are presentation defects rather than missing features.

### Design-system violations (fix before further UI work)

- [ ] **U1 — Archivo is never loaded.** `styles.css` declares `font-family: Archivo, Arial, sans-serif` with no `@font-face` or self-hosted file, so the app silently renders in Arial. The design system specifies Archivo for both heading and body. Self-host the font — a webfont CDN would break the offline-by-default trust boundary and the CSP.
- [ ] **U2 — Design tokens are hard-coded.** Every colour, space, and size in `styles.css` is a literal (`#ec3013`, `#f3f2f2`, `rgba(32,30,29,.4)`, px values). The system requires consuming `var(--color-*)`, `--font-*`, `--space-*`, `--radius-*`, `--shadow-*` from the token sheet so the theme can change in one place. Port the token layer from `mock-up/_ds/modernist-*/styles.css`.
- [ ] **U3 — Icons are Unicode glyphs, not Lucide.** `▤ ▯ ✎ ↻ ☷ ⌕ ✦ •••` render inconsistently across platforms and carry no consistent optical weight. The system specifies Lucide. Use inline SVG so nothing is fetched at runtime.
- [ ] **U4 — Accent contrast.** The accent-to-ground pair is tuned to ~3:1 — adequate for icons, large text, and chrome, not for body copy. Paragraph-size accent text must use `--color-accent-700`. Audit `.eyebrow` and `.paper-kicker`, which currently use `#ae1800` ad hoc.
- [ ] **U5 — `styles.css` is unmaintainable.** 18 lines holding the entire application stylesheet, with dozens of rules per line. Split by concern before it grows.

### Missing mockup surfaces

- [ ] **U6 — Annotation filter chips with live counts** (`All 41 / Claim 14 / Evidence 11 / Question 9 / Disagree 7`) and per-annotation `Add to note` / `Remember` actions.
- [ ] **U7 — Library as a real collection view**: Recents / Favourites / collections / Archive, and a sortable table with Title, Author, Ownership, Due, Last read. Currently a two-row hard-coded list.
- [ ] **U8 — Notes destination**: concept / source / scratch grouping, the scratch prompt-to-promote state, backlinks panel, "Prompts from this note", and Quick Copy.
- [ ] **U9 — Review session chrome**: `Card N of M · budget 20/day · elapsed`, Pause this prompt, Edit prompt, End session, "Your adopted answer" alongside the source, and the repeated-failure notice.
- [ ] **U10 — Settings**: only the AI & privacy page exists. Appearance, Reading, Annotations, Review, Storage, Export, and Shortcuts are inert labels. The Models table and Boundaries/Excluded-documents surfaces belong with task 7.1, not v1.
- [ ] **U11 — Import dialog**: show the computed fingerprint, the source path for open-in-place, and the destination path for managed copy.
- [ ] **U12 — Prompt editor**: add the prompt-type selector (Explanation / Application / Contrast / Cloze) and the "Add a cue" / "Keep anyway" affordances on lint warnings.
- [ ] **U13 — In-margin source note** with a truthful "Autosaved · N revisions kept" state. The AI margin-note variant is R5-only and must not appear as a disabled or empty v1 feature.
- [ ] **U14 — Reader toolbar**: fit-width and view-mode controls, and the continuous-scroll/spread indicator.
- [ ] **U16 — Fast annotation creation and management**: compact selection popover, keyboard-operable semantic colour/comment controls, locked highlight/underline mode with a persistent exit affordance, one-drag area capture, bookmark/comment creation, undo, and recoverable trash (FR-9.1–FR-9.2, FR-9.8).
- [ ] **U17 — Full search experience**: query editing, case/whole-word options, snippets, counts, previous/next traversal, keyboard focus, indexing-in-progress behaviour, low-confidence extraction disclosure, and no-results/error states (FR-8.3–FR-8.4).
- [ ] **U18 — Note authoring rather than note viewing**: new-note and template choice, source/concept/scratch type, concept-title guidance, formatting and link insertion, autosave/revision state, revision restore, split-with-links preview, scratch promotion/archive/discard, and source/user semantic roles (FR-10.3–FR-10.10).
- [ ] **U19 — Review queue, history, and synthesis**: due overview, daily budget/start controls, pause/priority/reschedule/retire, review history, empty and over-budget states, and the optional four-question reading-session synthesis flow. The active review card alone is insufficient.
- [ ] **U20 — Document lifecycle and recovery**: drag-drop target; first-page/loading/indexing states; duplicate confirmation; locate moved file; password, malformed, unsupported, scanned, and permission-denied outcomes; changed-version and re-anchoring review; corrupt-cache rebuild; and renderer failure with a safe retry/close path.
- [ ] **U21 — Export, backup, and restore**: Quick Copy feedback, Markdown/JSON/review/PDF export choices, destination conflict and diff/confirmation, progress/cancellation, atomic failure, backup contents, clean-profile restore progress, and restore verification results.
- [ ] **U22 — Empty and zero-data states**: Library, collections, Notes, annotations, search, Review, jobs, and Settings use real zero counts and a useful next action. No sample content or invented activity appears outside an unmistakably labelled prototype.
- [ ] **U23 — Adaptive reader layouts**: light/dark chrome and page dimming; 1024×640 deterministic pane collapse; user-resizable pane limits and restore; 100%–200% Windows scaling; long titles, CJK/RTL metadata, text scaling, and reduced-motion alternatives. The page canvas and primary controls must never become unreachable.
- [ ] **U24 — Shortcut discovery and semantic controls**: Settings → Shortcuts, contextual shortcut tooltips, unavailable-command explanations, logical focus movement, dialog focus trap/return, and native buttons/inputs/links instead of copying the mockup's clickable `div`/`span` markup.

### Prototype honesty

- [x] **U15 — The prototype currently asserts things that are not true.** The footer reads "41 annotations · 6 notes · Autosaved just now", the search box shows "7 / 41", and the review header claims "12 due · FSRS · desired retention 90%" — with no annotations, no notes, no autosave, no search index, and no scheduler behind any of it. Either wire these to real state or mark the build unmistakably as a mockup. Shipping invented counts makes it impossible to tell working features from placeholders during testing.
  - Verification note (2026-08-07): resolved by wiring to real state, completed by the C8/§2A work. The fabricated demo document and all invented annotations, notes, prompts, and search matches are deleted; the app opens to the Library with real zero counts. The footer shows live `annotationsList`/`notesList` counts and "All data stays on this device" instead of "Autosaved just now"; the search box starts empty with no auto-jump; the review header reads "0 due" with no FSRS/retention claims; Notes and Review render honest empty states that name the milestone their feature arrives with; the prompt dialog fields are empty placeholders instead of pre-filled sample text; the FR-8.4 copy warning now runs against live page text items. Verified by `tsc --noEmit`, `pnpm build`, and the 176/180 `pnpm test` run recorded under 2.1.
- [ ] **U25 — The mockup contradicts its own AI-off copy.** It says "Everything here is off by default" but its component property defaults `aiOn` to `true`. Set the reference state to off. For v1, remove R5-only AI navigation, model tables, generated drafts, and active AI chips from the implementation rather than presenting non-functional controls; retain them only in the separately identified R5 design reference.

---

## Completion gate

The product is not complete until every checkbox in phases 0–6, the R0 gate, and the applicable v1 UI/UX conformance backlog is checked and verified, and the PRD Appendix A journey passes end to end **with the network disabled and no AI models installed**:

install on a clean Windows machine → open a 400-page PDF via "Open with" → navigate by outline, search, and page number → highlight, label, and comment → capture a figure and add both to a source note → write a linked concept note → restart and restore the exact reading position → return from the note to each page annotation → mark one idea Remember and approve a prompt → complete a source-hidden review and self-rate → Quick Copy evidence with its page reference → export Markdown, review CSV/TSV, full backup, and an annotated PDF copy → restore that backup into a clean profile and repeat the note and review steps.

Only after that journey passes does task 7.1 (AI) become eligible to start.
