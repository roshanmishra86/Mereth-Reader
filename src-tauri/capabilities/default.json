{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities used by the main Reader window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
