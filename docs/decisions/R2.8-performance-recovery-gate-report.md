# R2 Performance & Recovery Gate Report (PRD §9.3, FR-9.8, FR-9.4, FR-7.3)

## Executive Summary
- **Date & Timestamp:** 2026-08-18 (task 3.8)
- **Gate Status:** PASSED ✅
- **Corpus:** corpus 15 fixtures; embedded_annotations sha256=8636a8ae
- **Methodology:** median of repeated in-process runs (webview-node: 50 creation-visibility samples + 20 filter samples; tauri-rust: 50 live typed inserts into a 10,000-row database); anchor/undo scenarios proven functionally against the shipped modules (annotationOverlay, annotationAnchor, versionAnchoring, annotationUndo, shortcutUtils) plus structural wiring scans.

---

## 1. System Hardware Profile
| Attribute | System Configuration |
| :--- | :--- |
| **CPU Model** | Intel(R) Core(TM) i7-10510U CPU @ 1.80GHz |
| **CPU Cores** | 8 |
| **Architecture** | x64 |
| **Platform** | linux (WSL2) |
| **Total Memory** | 7885 MB |

---

## 2. R2 Benchmarks (measured, not asserted)

| Metric | Target | Median | Worst | Samples | Measured by | Status |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **Annotation creation visibility** | `< 100 ms` | **0.035** | 2.272 | 50 | webview-node | PASSED ✅ |
| **Annotation creation durability** | `< 500 ms` | **0.154** | 2.193 | 50 | tauri-rust | PASSED ✅ |
| **Filter 10,000 annotations** | `< 100 ms` | **1.170** | 11.136 | 20 | webview-node | PASSED ✅ |

- Creation visibility: the worst-case webview pipeline (record builder + checksum + IPC serialization of a multi-KB quote).
- Creation durability: the Rust typed insert into an in-memory database already holding 10,000 annotation rows (live cargo gate test `test_db_add_annotation_durability_budget_at_10k_rows`).
- Filter: the 3.7 `applyAnnotationFilters` with every criterion enabled over a synthetic 10,000-annotation corpus.

---

## 3. Anchor preservation (§9.3)

| Scenario | Geometry result | Review | Mechanism |
| :--- | :--- | :--- | :--- |
| **zoom** | preserved | none needed | geometry re-derived from stored normalized rects at every scale (denormalizeGeometry linearity, verified ×2 exact) |
| **rotation** | preserved | none needed | normalized↔viewport transform is an exact algebraic inverse at 0°/90°/180°/270° (round-trip identity, verified) |
| **restart** | preserved | none needed | records persist normalized rects; serialization round-trip reproduces identical pixels (verified) |
| **renderer-reload** | preserved | none needed | overlay input is only normalized rects + viewport base size; fresh measurements reproduce identical pixels (verified) |
| **incompatible-replacement** | not attached | explicit review before reuse | FR-7.3: bytes changed → explicit re-anchor review; unmatched quotes detach (selectReanchorActions verified, guard asserted), old coordinates are never reattached silently |

---

## 4. Undo paths (FR-9.8)

| Path | Status | Mechanism |
| :--- | :--- | :--- |
| **pointer** | verified | Undo button in the annotations pane → onUndoAnnotation → AnnotationUndoManager create/edit/trash inverses (inverses verified; binding scan-verified) |
| **keyboard** | verified | Ctrl+Z resolves to annot.undo (resolveShortcutAction verified) → same onUndoAnnotation flow (routing scan-verified) |

---

## 5. Verification Conclusion
All R2 performance and recovery gate targets have been measured and validated.
**Failures block R3 work**; passing entries record corpus and hardware details per §9.3.
